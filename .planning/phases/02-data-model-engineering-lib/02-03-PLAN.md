---
phase: 02-data-model-engineering-lib
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/train.py
  - lib/evaluate.py
  - lib/registry.py
  - tests/lib/test_train.py
  - tests/lib/test_evaluate.py
  - tests/lib/test_registry.py
autonomous: true
requirements: [REQ-D1, REQ-D2, REQ-D3]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`chronological_split` partitions on each row's own `tpep_pickup_datetime` value, never on which monthly file the row came from — real TLC monthly files contain rows whose pickup timestamp falls years outside their nominal month, and splitting by file would leak those rows into the wrong side."
    - "The split boundary is half-open and specified: a row whose `tpep_pickup_datetime` equals `SPLIT_TIMESTAMP` exactly lands in the test partition, and a row one nanosecond earlier lands in the training partition; the two partitions are disjoint and their row counts sum to the input row count."
    - "`chronological_split` raises `ValueError` naming the empty side when the boundary leaves either partition with zero rows, rather than returning an empty frame that a downstream fit would fail on obscurely."
    - "`train_trip_duration_model` is deterministic: two calls on identical inputs produce element-wise identical predictions, because `LGBM_PARAMS` fixes `random_state` and uses a single-threaded, deterministic configuration."
    - "The categorical dtype used for `PULocationID`/`DOLocationID` enumerates the full 1-263 zone range rather than only the categories present in the training split, so a zone that appears solely in the test window is scored as that zone instead of silently becoming a missing value."
    - "`rmse` returns the exact expected float for a hand-computed input pair, and `evaluate_model` reports both the RMSE and the row count it was computed over."
    - "`beats_champion(candidate, None)` is `True` — the first-ever run with no registered champion promotes — and `beats_champion(candidate, champion)` is `True` only when the candidate RMSE is strictly lower, so an exactly-tied candidate does not displace the incumbent."
    - "`ModelRegistry.get_champion_rmse()` returns `None` when the injected client raises `MlflowException` for a missing alias, and the float value of the version's rmse tag otherwise; every registry test runs against a `MagicMock`, never a live tracking server."
    - "`promote_to_champion` and `set_candidate` reach MLflow through `set_registered_model_alias` with the aliases `champion` and `candidate`, matching REQ-D3's alias-based promotion contract."
    - statement: "A reviewer reading `lib/train.py` can tell within one screen which hyperparameters were chosen and why they are fixed, satisfying REQ-D2's `single fixed LightGBM config committed` in a form a human can audit."
      verification: backstop
  artifacts:
    - lib/train.py
    - lib/evaluate.py
    - lib/registry.py
    - tests/lib/test_train.py
    - tests/lib/test_evaluate.py
    - tests/lib/test_registry.py
  key_links:
    - "`lib.features.FEATURE_COLUMNS` / `TARGET_COLUMN` into `lib.train.train_trip_duration_model` — tests must import the contract rather than restating the column list"
    - "`lib.registry.ModelRegistry.get_champion_rmse` into `lib.evaluate.beats_champion` — the None-means-no-champion contract that Phase 3's conditional promotion branch will compile against"
    - "`lib.ingest.TLC_START_MONTH` / `TLC_END_MONTH` into `lib.train.SPLIT_TIMESTAMP` — the split boundary must sit inside the pinned window and after the drift onset"
  prohibitions:
    - statement: "No hyperparameter search, sweep, or auto-tuning library may enter the repository; REQ-D2 asks for one committed fixed configuration and PROJECT.md calls tuning out as explicitly deferred theatre."
      status: active
      verification: explicit
    - statement: "The train/test split must not be random or stratified; a random split averages across the March-2020 demand collapse and hides exactly the drift signal the 12-month window was chosen to expose."
      status: active
      verification: explicit
    - statement: "Model promotion must not use MLflow's deprecated numeric-stage transition surface; REQ-D3 requires the 3.x alias API, and the deprecated call would silently work today and break on the next major."
      status: active
      verification: explicit
---

<objective>
Build out the modelling tail of `lib/`: a chronological train/test split honouring D-08, a single fixed untuned LightGBM configuration honouring REQ-D2, RMSE-based evaluation with a champion comparison, and an MLflow registry wrapper using alias-based promotion honouring REQ-D3 — all unit-tested against synthetic frames and a mocked client.

Purpose: this is the half of Phase 2 that needs neither the downloaded Parquet cache nor the zone-centroid table, so it can be proven in parallel with data acquisition. It also fixes the champion-lookup contract that Phase 3's conditional promotion branch compiles against, so getting the None-means-no-champion semantics right now avoids reworking the DAG later.

Output: expanded `lib/train.py`, `lib/evaluate.py`, `lib/registry.py`, and three dedicated test modules.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-model-engineering-lib/02-CONTEXT.md
@.planning/phases/02-data-model-engineering-lib/02-RESEARCH.md
@.planning/phases/02-data-model-engineering-lib/02-PATTERNS.md
@.planning/phases/02-data-model-engineering-lib/02-01-SUMMARY.md
@CLAUDE.MD
</context>

## Artifacts this phase produces

Full Phase 2 inventory; this plan creates or expands the items marked **[02-03]**.

**`lib/` modules and public symbols**

- `lib/ingest.py` — `TLC_START_MONTH`, `TLC_END_MONTH`, `TLC_DATA_DIR`, `DEFAULT_BATCH_SIZE`, `month_parquet_path(month, data_dir)`, `read_month_chunked(path, batch_size)`, `add_trip_duration(df)`, `FilterReport`, `filter_trip_quality(df)`, `load_month(month, data_dir, batch_size)`
- `lib/schemas.py` — `VALID_ZONE_MIN`, `VALID_ZONE_MAX`, `PLACEHOLDER_ZONE_IDS`, `PASSENGER_COUNT_MIN`, `PASSENGER_COUNT_MAX`, `trip_schema`, `validate_trips(df)`
- `lib/features.py` — `EARTH_RADIUS_KM`, `ZONE_CENTROID_PATH`, `FEATURE_COLUMNS`, `TARGET_COLUMN`, `ZONE_CATEGORY_DTYPE`, `load_zone_centroids(path)`, `haversine_km(lat1, lon1, lat2, lon2)`, `haversine_km_rowwise(df)`, `join_zone_centroids(df, centroids)`, `downcast_features(df)`, `build_features(df, centroids)`
- `lib/train.py` **[02-03: `SPLIT_TIMESTAMP`, `chronological_split`, `save_model`, `load_booster`; hardens `LGBM_PARAMS`, `CATEGORICAL_FEATURES`, `train_trip_duration_model`]**
- `lib/evaluate.py` **[02-03: `EvaluationResult`, `evaluate_model`, `beats_champion`; hardens `rmse`]**
- `lib/registry.py` **[02-03: `CANDIDATE_ALIAS`, `tag_version_rmse`, `set_candidate`, `promote_to_champion`; hardens `ModelRegistry.get_champion_rmse`]**

**Scripts (CLI)**

- `scripts/download_tlc_data.py`, `scripts/precompute_zone_centroids.py` (plan 02-02); `scripts/benchmark_features.py` (plan 02-05)

**Data / config files**

- `data/zone_centroids.csv` (committed, 263 rows) and `data/tlc/*.parquet` (gitignored cache) — plan 02-02

**Tests**

- `tests/lib/test_tracer_end_to_end.py` (02-01); `tests/lib/test_train.py`, `tests/lib/test_evaluate.py`, `tests/lib/test_registry.py` **[02-03]**; `tests/lib/test_ingest.py`, `tests/lib/test_schemas.py` (02-04); `tests/lib/test_features.py` (02-05)

**Modified existing files**

- `pyproject.toml`, `uv.lock`, `scripts/qa.sh`, `.github/workflows/ci.yml`, `.gitignore` (02-01); `README.md` (02-05)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chronological train/test split across the drift boundary</name>
  <files>lib/train.py, tests/lib/test_train.py</files>
  <read_first>
    - `lib/train.py` — the tracer's current contents, so the expansion matches what is already there
    - `lib/ingest.py` — `TLC_START_MONTH` and `TLC_END_MONTH`, which bound where `SPLIT_TIMESTAMP` may legally sit
    - `lib/months.py` — module docstring and `raise ValueError(f"... {value!r}")` error convention to mirror
    - `tests/lib/test_months.py` — test style: one-line module docstring, plain annotated `def test_x() -> None:` functions, exact-value asserts, `pytest.raises` on error paths
    - `.planning/phases/02-data-model-engineering-lib/02-CONTEXT.md` section `### Train/Test Split` (D-08 and why a random split was rejected)
    - `.planning/phases/02-data-model-engineering-lib/02-RESEARCH.md` section `### Pitfall 9` (rows whose pickup timestamp falls outside their file's nominal month)
  </read_first>
  <behavior>
    - A frame of 6 rows with pickup timestamps straddling the boundary splits into the expected two frames, asserted by exact row index sets.
    - A row whose `tpep_pickup_datetime` equals `SPLIT_TIMESTAMP` exactly appears in the test partition, not the training partition.
    - A row one nanosecond before `SPLIT_TIMESTAMP` appears in the training partition.
    - `len(train) + len(test)` equals `len(df)` and the two index sets are disjoint for a randomised-order input frame.
    - Input rows are not required to be sorted: an input frame in shuffled timestamp order splits identically to the same frame sorted ascending.
    - A frame whose rows all precede the boundary raises `ValueError` whose message names the empty test side; a frame whose rows all follow it raises `ValueError` naming the empty train side.
    - An empty input frame raises `ValueError` rather than returning two empty frames.
    - `SPLIT_TIMESTAMP` falls strictly inside the pinned window and on or after 2020-03-01.
  </behavior>
  <action>
Expand `lib/train.py` with the D-08 chronological split.

Add `SPLIT_TIMESTAMP = pd.Timestamp("2020-03-01")` with a short comment recording the reasoning: the pinned window runs 2019-07 through 2020-06, and this boundary puts the eight pre-collapse months in training and the four COVID-affected months in evaluation, which is the drift demonstration REQ-D1 and D-08 exist to produce.

Add `chronological_split(df: pd.DataFrame, split_ts: pd.Timestamp = SPLIT_TIMESTAMP) -> tuple[pd.DataFrame, pd.DataFrame]`. Partition on each row's own `tpep_pickup_datetime` value using a boolean mask, never on the source file or on row position: RESEARCH.md's Pitfall 9 found rows dated 2018-11 and even 2003-01 inside the January-2019 file, so file-derived partitioning leaks. Use a half-open boundary — rows strictly before `split_ts` are training, rows at or after it are test — and state that convention in the function docstring so the tie case is documented rather than incidental. Preserve each partition's original row order and reset neither index silently; return copies so the caller's frame is untouched.

Raise `ValueError` naming which side came out empty when either partition has zero rows, following `lib/months.py`'s convention of a plain built-in exception with an f-string message carrying the offending value in `!r` form. An empty partition is always a configuration error here: a downstream fit on it fails with an opaque LightGBM message instead.

Write the tests first per the `<behavior>` list, using tiny synthetic frames built inline with `pd.DataFrame` and `pd.Timestamp`, per D-07's rule that the fast suite never touches the real download.
  </action>
  <acceptance_criteria>
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml pytest tests/lib/test_train.py --no-cov -q` exits 0.
    - A test asserts that a row with `tpep_pickup_datetime` exactly equal to `SPLIT_TIMESTAMP` is present in the test partition and absent from the training partition.
    - A test asserts a shuffled-order input frame yields the same two partitions as the sorted-order equivalent, compared with `pandas.testing.assert_frame_equal` after sorting.
    - Two `pytest.raises(ValueError)` tests cover the all-before and all-after boundary cases, and one covers an empty input frame.
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml python -c "import pandas as pd; from lib.train import SPLIT_TIMESTAMP; from lib.ingest import TLC_START_MONTH, TLC_END_MONTH; assert pd.Timestamp(TLC_START_MONTH) is not None; assert SPLIT_TIMESTAMP.year==2020 and SPLIT_TIMESTAMP.month==3; print('split-ts-ok')"` prints `split-ts-ok`.
    - The `chronological_split` docstring states the half-open boundary convention in words.
  </acceptance_criteria>
  <verify>
    <automated>UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml pytest tests/lib/test_train.py --no-cov -q &amp;&amp; scripts/qa.sh typecheck</automated>
  </verify>
  <done>`chronological_split` partitions by event timestamp with a documented half-open boundary, refuses to produce an empty side, and its tests pin the tie case and the shuffled-order case explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixed LightGBM configuration, stable categoricals, and safe model serialization</name>
  <files>lib/train.py, tests/lib/test_train.py</files>
  <read_first>
    - `lib/train.py` — the tracer's `LGBM_PARAMS`, `CATEGORICAL_FEATURES` and `train_trip_duration_model`
    - `lib/features.py` — `FEATURE_COLUMNS`, `TARGET_COLUMN` and `ZONE_CATEGORY_DTYPE`; tests must import these rather than restate the column list
    - `.planning/phases/02-data-model-engineering-lib/02-RESEARCH.md` sections `### Pattern 4: LightGBM 4.7.0 minimal fixed config (REQ-D2)` including its verified categorical-alignment pitfall, and `### Known Threat Patterns for this stack` (the deserialization row)
    - `.planning/phases/02-data-model-engineering-lib/02-CONTEXT.md` section `### Claude's Discretion` (hyperparameter values are the executor's call, the fixedness is not)
    - `.planning/REQUIREMENTS.md` REQ-D2 row (acceptance criterion wording)
  </read_first>
  <behavior>
    - `train_trip_duration_model` returns a fitted regressor whose `predict` yields one finite value per input row for a 60-row synthetic frame.
    - Two calls with identical inputs produce element-wise identical predictions, asserted with `numpy.testing.assert_array_equal`.
    - The zone categorical dtype enumerates all 263 zone ids, so a frame containing a zone absent from the training rows still scores without producing a NaN feature.
    - `save_model` then `load_booster` round-trips: the reloaded booster's predictions match the in-memory model's predictions for the same input.
    - The persisted model file's first bytes are LightGBM's text format, not a binary pickle header.
    - `LGBM_PARAMS` contains `random_state` and does not vary between imports.
  </behavior>
  <action>
Harden `lib/train.py`'s modelling surface.

Pin `LGBM_PARAMS` as a module-level dict holding exactly one configuration and document each value in a short block comment naming why it is fixed: `objective="regression"` and `metric="rmse"` state the loss explicitly rather than relying on defaults; `num_leaves=31` is LightGBM's own documented default and is left alone deliberately; `learning_rate=0.05` with `n_estimators=300` is a conventional slow-and-steady pairing; `random_state=42`, `n_jobs=1` and `deterministic=True` make runs byte-reproducible, which the determinism test depends on; `verbose=-1` keeps the test suite output readable. Introduce no hyperparameter-search, sweep, or auto-tuning library, and add no cross-validation grid — REQ-D2's acceptance criterion is a single committed configuration with no tuning framework present anywhere in the repository.

Add `ZONE_CATEGORY_DTYPE` in `lib/features.py`'s namespace if the tracer did not already define it, and use it here: build the categorical columns from an explicit `pd.CategoricalDtype(categories=range(1, 264))` rather than from whatever values happen to appear in the training split. RESEARCH.md's Context7-sourced note is explicit that LightGBM aligns pandas categories observed during training and treats unseen categories at prediction time as missing — with a split-derived category set, a zone that only appears in the post-COVID evaluation window would silently become a missing value instead of a real zone.

Keep `train_trip_duration_model(x: pd.DataFrame, y: pd.Series) -> lgb.LGBMRegressor` operating on a copy so the caller's frame is not mutated by the categorical cast, and pass `categorical_feature=CATEGORICAL_FEATURES` at fit time.

Add `save_model(model: lgb.LGBMRegressor, path: Path) -> None` and `load_booster(path: Path) -> lgb.Booster` using LightGBM's own text-format serialization (`model.booster_.save_model(...)` and `lgb.Booster(model_file=...)`). Do not reach for a general Python object-serialization module: a model artifact that Phase 3 will register in MLflow and later pull from a different service crosses a trust boundary, and LightGBM's native text format carries no code-execution surface. Create the parent directory if it does not exist, and raise `FileNotFoundError` from the loader when the path is absent rather than returning an empty booster.

Write the tests first per the `<behavior>` list, using a 60-row synthetic feature frame assembled from `lib.features.FEATURE_COLUMNS` and `TARGET_COLUMN` and `tmp_path` for the round-trip.
  </action>
  <acceptance_criteria>
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml pytest tests/lib/test_train.py --no-cov -q` exits 0.
    - `grep -rniE 'optuna|hyperopt|ray\.tune|GridSearchCV|RandomizedSearchCV|BayesSearchCV|katib' --include='*.py' --include='*.toml' --include='*.yml' lib scripts tests components pipelines pyproject.toml` produces no output.
    - A determinism test calls `train_trip_duration_model` twice on the same inputs and asserts identical predictions via `numpy.testing.assert_array_equal`.
    - A test asserts the categorical dtype used for `PULocationID` has exactly 263 categories spanning 1 through 263.
    - A round-trip test asserts `load_booster(save_model(...))` predictions equal the in-memory model's predictions, and asserts the saved file's first line starts with LightGBM's text-format tree header rather than a binary pickle protocol marker.
    - `pytest.raises(FileNotFoundError)` covers `load_booster` on a missing path.
    - `scripts/qa.sh typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml pytest tests/lib/test_train.py --no-cov -q &amp;&amp; scripts/qa.sh typecheck &amp;&amp; test -z "$(grep -rniE 'optuna|hyperopt|ray\.tune|GridSearchCV|RandomizedSearchCV|BayesSearchCV|katib' --include='*.py' --include='*.toml' lib scripts tests components pipelines pyproject.toml)"</automated>
  </verify>
  <done>One documented fixed hyperparameter set trains deterministically over a full-zone-range categorical dtype, the model persists and reloads through LightGBM's own text format, and no tuning machinery exists anywhere in the tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RMSE evaluation and alias-based champion/candidate registry</name>
  <files>lib/evaluate.py, lib/registry.py, tests/lib/test_evaluate.py, tests/lib/test_registry.py</files>
  <read_first>
    - `lib/evaluate.py` and `lib/registry.py` — the tracer's current contents
    - `.planning/phases/02-data-model-engineering-lib/02-RESEARCH.md` section `### Pattern 5: MLflow registry client wrapper, mockable (REQ-D3)` including its mocked-client test snippets, and `### Pitfall 10` (the 100% branch-coverage gate applies to every exception path)
    - `.planning/research/STACK.md` MLflow row — 3.x replaced registry stages with aliases; the alias API is the current surface
    - `.planning/REQUIREMENTS.md` REQ-D3 row (acceptance criterion wording)
    - `.planning/phases/02-data-model-engineering-lib/02-CONTEXT.md` `<domain>` section — this phase's registry wrapper is tested against a mocked client only; no tracking server exists until Phase 3
  </read_first>
  <behavior>
    - `rmse([3.0, 5.0], [4.0, 5.0])` equals the exact hand-computed value, asserted without tolerance slack beyond floating-point equality helpers.
    - `rmse` on mismatched-length inputs raises `ValueError`.
    - `evaluate_model` returns an `EvaluationResult` whose `n_rows` equals the test frame's row count and whose `rmse` matches a direct `rmse` call on the same predictions.
    - `beats_champion(10.0, None)` is `True`; `beats_champion(10.0, 12.0)` is `True`; `beats_champion(12.0, 10.0)` is `False`; `beats_champion(10.0, 10.0)` is `False`.
    - `ModelRegistry.get_champion_rmse()` is `None` when the mocked client raises `MlflowException`.
    - `ModelRegistry.get_champion_rmse()` equals the float of the version's rmse tag when the mocked client returns a tagged version.
    - `promote_to_champion("3")` calls the client's alias setter once with the model name, the champion alias, and `"3"`.
    - `set_candidate("4")` calls the alias setter once with the candidate alias.
    - `tag_version_rmse("4", 12.5)` calls the client's version-tag setter with the rmse tag key and the stringified value.
  </behavior>
  <action>
Expand `lib/evaluate.py` and `lib/registry.py` into their full Phase 2 surface.

In `lib/evaluate.py`: keep `rmse(y_true, y_pred) -> float` numpy-based and returning a plain Python float, and raise `ValueError` on a length mismatch rather than letting numpy broadcast silently. Add a frozen dataclass `EvaluationResult` with fields `rmse: float` and `n_rows: int`. Add `evaluate_model(model, x_test, y_test) -> EvaluationResult` that predicts and packages both numbers — the row count matters because a champion comparison computed over a handful of rows is not comparable to one over a month. Add `beats_champion(candidate_rmse: float, champion_rmse: float | None) -> bool` returning `True` when there is no champion yet, and otherwise `True` only on a strictly lower candidate RMSE; a tie leaves the incumbent in place, which is the conservative reading of REQ-B7's "beats" and avoids alias churn on a re-run of identical data. Keep this module free of any MLflow import so it stays testable without a client — the champion number arrives as a plain float argument, which is the seam the Architectural Responsibility Map specifies.

In `lib/registry.py`: add `CANDIDATE_ALIAS = "candidate"` alongside the existing `CHAMPION_ALIAS` and `RMSE_TAG`. Keep the injected-client constructor. Add `tag_version_rmse(version: str, value: float) -> None` calling the client's model-version tag setter with `RMSE_TAG` and the stringified value; `set_candidate(version: str) -> None` and `promote_to_champion(version: str) -> None`, both calling `set_registered_model_alias(model_name, alias, version)` with the candidate and champion aliases respectively. Use only MLflow 3.x's alias surface — `set_registered_model_alias` and `get_model_version_by_alias`; the deprecated numeric-stage transition call would appear to work today and break on the next major, and REQ-D3's acceptance criterion names the alias API specifically. Keep `get_champion_rmse` returning `None` on `MlflowException` and never swallowing other exception types.

Write `tests/lib/test_evaluate.py` and `tests/lib/test_registry.py` first per the `<behavior>` list. Every registry test injects a `unittest.mock.MagicMock` client and asserts with `assert_called_once_with`, per RESEARCH.md Pattern 5. Cover every exception branch explicitly: `pyproject.toml` enforces 100% branch coverage with `branch = true`, so an untested `MlflowException` handler fails the entire suite rather than just its module.
  </action>
  <acceptance_criteria>
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml pytest tests/lib/test_evaluate.py tests/lib/test_registry.py --no-cov -q` exits 0.
    - A test asserts all four `beats_champion` cases including the exact-tie case returning `False`.
    - A test asserts `get_champion_rmse()` is `None` when the mocked client raises the MLflow exception type, and another asserts the float path.
    - Tests assert the alias setter was called once with the literal aliases `champion` and `candidate` respectively.
    - `grep -rn 'transition_model_version_stage' lib scripts tests` produces no output.
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml python -c "import ast; t=ast.parse(open('lib/evaluate.py').read()); mods=[(n.module or '') for n in ast.walk(t) if isinstance(n, ast.ImportFrom)]+[a.name for n in ast.walk(t) if isinstance(n, ast.Import) for a in n.names]; assert not any(m.split('.')[0].lower()=='mlflow' for m in mods), mods; print('evaluate-decoupled-ok')"` prints `evaluate-decoupled-ok`, proving the evaluate module has no client coupling.
    - `scripts/qa.sh test` exits 0, meaning `lib/train.py`, `lib/evaluate.py` and `lib/registry.py` all hold 100% branch coverage alongside the tracer test.
    - `scripts/qa.sh typecheck` and `scripts/qa.sh boundary` exit 0, and `ruff check`/`ruff format --check` are clean over `lib` and `tests`. Lint is scoped to `lib` and `tests` rather than run repo-wide here because wave-2 sibling plan 02-02 is concurrently writing files under `scripts/`; the repo-wide `scripts/qa.sh lint` and `format` gates run in waves 3 and 4, after both wave-2 plans have landed.
  </acceptance_criteria>
  <verify>
    <automated>UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml ruff check lib tests &amp;&amp; UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml ruff format --check lib tests &amp;&amp; scripts/qa.sh typecheck &amp;&amp; scripts/qa.sh test &amp;&amp; scripts/qa.sh boundary &amp;&amp; test -z "$(grep -rn 'transition_model_version_stage' lib scripts tests)"</automated>
  </verify>
  <done>Evaluation returns an RMSE plus row count, champion comparison handles the no-champion and tie cases explicitly, the registry wrapper drives MLflow purely through the 3.x alias API against a mocked client, and the full quality-gate suite passes at 100% branch coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Serialized model artifact to `lib/train.py` loader | Model bytes become an in-process object graph; in Phase 3 those bytes will have travelled through MLflow and an object store |
| `lib/registry.py` to the MLflow tracking service | An external service's API surface, mocked in this phase and real in Phase 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering / Elevation of Privilege | `lib/train.py` model persistence and loading | high | mitigate | Persist and load exclusively through LightGBM's native text format (`booster_.save_model` / `lgb.Booster(model_file=...)`); no general object-deserialization module is imported, so a tampered artifact cannot execute code on load |
| T-02-02 | Information Disclosure | `lib/registry.py` credential handling | low | accept | Phase 2 injects the client through the constructor and handles no tracking URI, token or S3 credential; real credential handling is scoped to Phase 3 when a server exists |
| T-02-09 | Repudiation | Champion promotion without a recorded metric | low | mitigate | `tag_version_rmse` records the RMSE on the model version, so a later reviewer can reconstruct why a version was promoted rather than trusting the alias alone |
</threat_model>

<verification>
- `scripts/qa.sh typecheck`, `test` and `boundary` exit 0, and `ruff check`/`ruff format --check` are clean over `lib` and `tests`. The repo-wide lint gate is deliberately deferred to wave 3, because wave-2 sibling plan 02-02 writes files under `scripts/` concurrently.
- No tuning framework or deprecated stage-transition call exists anywhere under `lib/`, `scripts/`, `tests/`, `components/` or `pipelines/`.
- `lib/evaluate.py` imports nothing from MLflow.
- Every registry test runs against a `MagicMock`; no network call is attempted.
</verification>

<success_criteria>
- `chronological_split` splits on event timestamp with a documented half-open boundary and refuses an empty side.
- `LGBM_PARAMS` is one documented fixed configuration producing deterministic predictions over a full 1-263 zone categorical dtype.
- Models persist and reload through LightGBM's text format.
- `beats_champion` and `ModelRegistry` implement REQ-D3's alias-based promotion contract, fully covered including exception branches.
</success_criteria>

<output>
Create `.planning/phases/02-data-model-engineering-lib/02-03-SUMMARY.md` when done. Record the final `LGBM_PARAMS` values with their one-line rationales (this is the text README's model section will quote), the chosen `SPLIT_TIMESTAMP`, and the exact `ModelRegistry` method signatures Phase 3's promotion component will call.
</output>
