---
phase: 03-kubeflow-pipeline-core-deployment
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - deploy/01-cluster-up.sh
  - deploy/02-kfp-install.sh
  - deploy/03-storage-tracking.sh
  - deploy/04-upload-raw-data.sh
  - deploy/minio-values.yaml
  - deploy/mlflow-values.yaml
  - scripts/submit_pipeline.py
  - scripts/backfill_checksums.py
autonomous: true
requirements: [REQ-B1, REQ-B3]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`kubectl get pods -n kubeflow` shows the KFP standalone control plane Running, and `kubectl get namespace istio-system` reports NotFound — KFP standalone, not the multi-user distribution (REQ-B1)."
    - "Every `ghcr.io/kubeflow/kfp-*` image running in the cluster carries the pinned `2.17.0` tag, not a floating branch tag: the `env/platform-agnostic` overlay was used, not `env/dev` (03-RESEARCH.md Pitfall 9)."
    - "MinIO and MLflow run as separate pods in the `mlflow` namespace, MLflow backed by a SQLite store on a PVC and by MinIO for artifacts — the D-12 topology, not KFP's own bundled object store."
    - "The buckets `tlc-raw`, `mlflow-artifacts` and `backfill` all exist in the dedicated MinIO instance."
    - "The Secret `mlflow-minio-creds` exists in both the `mlflow` and `kubeflow` namespaces, because KFP standalone runs task pods in `kubeflow` and `use_secret_as_env` resolves the Secret in the task pod's own namespace."
    - "No credential value appears in any tracked file, in any `helm --set` argument, or in any script's stdout: credentials are generated or read from the environment and land only in Kubernetes Secrets."
    - "Every service address a pod uses is an in-cluster service DNS name; host port-forward addresses appear only in host-side tooling (03-RESEARCH.md / PITFALLS.md Pitfall 7)."
    - "`scripts/submit_pipeline.py` compiles with `COMPONENT_IMAGE_TAG` set to a real git SHA and submits the compiled package to the KFP API, so the run under test references exactly the images CI published for that commit."
    - "A submitted run's per-month branch completes for every month in the range: each task pod pulled its GHCR image successfully, and a `backfill/v1/features/<month>.parquet` object exists in MinIO for each month (PITFALLS.md Pitfall 3 — a successful docker push is not proof a pod can pull)."
    - "Every task pod carries a real `resources` block with memory requests and limits when inspected with `kubectl get pod -o yaml`, not merely a pipeline that compiled without error (PITFALLS.md Pitfall 2)."
  artifacts:
    - path: "deploy/01-cluster-up.sh"
      provides: "k3d install (user-local, no sudo) and cluster creation with an advisory memory budget"
    - path: "deploy/02-kfp-install.sh"
      provides: "KFP standalone 2.17.0 install via the platform-agnostic overlay, plus the REQ-B1 and Pitfall 9 verification block"
    - path: "deploy/03-storage-tracking.sh"
      provides: "Helm install, Secret creation in both namespaces, dedicated MinIO pod and in-cluster MLflow (D-12)"
    - path: "deploy/minio-values.yaml"
      provides: "MinIO chart values: standalone mode, explicit small memory request, persistence, the three buckets, existingSecret"
    - path: "deploy/mlflow-values.yaml"
      provides: "MLflow chart values: SQLite backend store on a PVC, MinIO artifact root, resource limits, Secret-referenced S3 credentials"
    - path: "deploy/04-upload-raw-data.sh"
      provides: "Uploads the locally cached TLC months into the tlc-raw bucket so component pods can read them"
    - path: "scripts/submit_pipeline.py"
      provides: "Host-side compile-and-submit against the KFP API with a git-SHA image tag"
    - path: "scripts/backfill_checksums.py"
      provides: "Host-side sha256 of every deterministic backfill key for a month range, used by REQ-B8's proof"
  key_links:
    - from: "deploy/03-storage-tracking.sh"
      to: "pipelines/train_pipeline.py"
      via: "the mlflow-minio-creds Secret name and key names the pipeline's use_secret_as_env expects"
      pattern: "mlflow-minio-creds"
    - from: "scripts/submit_pipeline.py"
      to: "pipelines/compile.py"
      via: "compiles the package with COMPONENT_IMAGE_TAG before submitting"
      pattern: "COMPONENT_IMAGE_TAG"
    - from: "deploy/04-upload-raw-data.sh"
      to: "lib/artifacts.py"
      via: "uploads under the exact object name lib.ingest.month_parquet_path expects"
      pattern: "yellow_tripdata_"
---

<objective>
Stand up the cluster half of the phase: k3d, KFP standalone 2.17.0 on the platform-agnostic overlay, a dedicated MinIO pod and an in-cluster MLflow (D-12), the credentials plumbing, the raw-data upload, and the host-side submit and checksum tooling — then prove a real run's per-month branch executes with GHCR-pulled images.

Purpose: this is the highest-risk, least-familiar work in the milestone (PROJECT.md names Kubernetes as the explicit skill gap). D-10 chose to attempt it directly under a time-box rather than spike it separately, with `research/ARCHITECTURE.md`'s safe cut line pre-specified as the fallback.
Output: four `deploy/` scripts, two Helm values files, two host-side Python scripts, and a running cluster with one proven per-month branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-kubeflow-pipeline-core-deployment/03-CONTEXT.md
@.planning/phases/03-kubeflow-pipeline-core-deployment/03-RESEARCH.md
@.planning/phases/03-kubeflow-pipeline-core-deployment/03-02-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

## Artifacts this phase produces

See `03-01-PLAN.md`'s inventory. This plan creates the entire `deploy/` tree plus `scripts/submit_pipeline.py` and `scripts/backfill_checksums.py`.

## The D-10 time-box and its fallback

CONTEXT.md's D-10 chose to attempt the cluster install directly, with a firm time-box and a pre-specified fallback rather than a dedicated spike. Honour it literally:

- Time-box Task 1 and Task 2 together to roughly three hours of wall-clock effort.
- If the cluster is still not usable at that point, **stop**. Do not keep grinding. Record in the SUMMARY the exact failing command, its output, and the last step that did work, then halt and escalate to the developer rather than continuing to spend the phase budget.
- The fallback is already fully specified and needs no rework to invoke: `research/ARCHITECTURE.md`'s safe cut line treats a fully-tested `lib/`, green CI, published component images and at least one component proven end-to-end on the cluster as defensible partial progress, not a failed phase. Plans 03-01 through 03-03 already deliver everything except that last item, which is Task 3 here.
- The two `reversible` escape hatches recorded in CONTEXT.md remain available and cost no rework: MLflow can move to a host process talking to the same tracking URI (D-12), and the backfill proof already runs on a 2-3 month subset (D-13).

## Environment facts established by research

`03-RESEARCH.md`'s Environment Availability table measured this machine: Docker 29.1.3 and `kubectl` v1.35.4 (with kustomize v5.7.1 bundled, so `kubectl apply -k` needs no standalone kustomize binary) are present; **k3d and helm are not installed** and Task 1 and Task 2 install them; roughly 10GiB of 15GiB RAM is free at idle, which is the binding constraint PITFALLS.md Pitfall 1 covers.

Two research open questions to resolve at execution time rather than guess:

- The exact published tag of the MLflow OCI Helm chart — resolve it with `helm show chart oci://ghcr.io/mlflow/charts/mlflow` and pin whatever it reports.
- Whether the MLflow chart's `runAsNonRoot` / `readOnlyRootFilesystem` defaults and its `fsGroup` let the server write `/mlflow/mlflow.db` on the PVC. If the pod crash-loops with a SQLite "unable to open database file" error, override `podSecurityContext.fsGroup` in the values file. Check this in the first minutes after install, not after building on top of it.

<tasks>

<task type="auto">
  <name>Task 1: k3d cluster and KFP standalone 2.17.0</name>
  <precondition>`docker info` exits 0 (the Docker daemon is running), and `free -g` reports at least 8 GiB available — k3d plus the KFP control plane will not fit otherwise (PITFALLS.md Pitfall 1).</precondition>
  <reversibility rating="reversible">D-10's install attempt is time-boxed against a pre-specified fallback; `k3d cluster delete` returns the machine to its prior state with no repository change to undo.</reversibility>
  <files>deploy/01-cluster-up.sh, deploy/02-kfp-install.sh</files>
  <read_first>
    - `.planning/research/STACK.md` — the Installation section's exact `k3d cluster create` and `kubectl apply -k` command sequence, and the pinned versions (KFP 2.17.0, k3d v5.9.0)
    - `.planning/phases/03-kubeflow-pipeline-core-deployment/03-RESEARCH.md` — Pitfall 9 (why the platform-agnostic overlay, and the post-install tag verification), and the Environment Availability table
    - `.planning/research/PITFALLS.md` — Pitfall 1 (k3d memory flags are advisory, not a hard cgroup cap) and Pitfall 3 (a successful push is not proof of a cluster-side pull)
    - `scripts/qa.sh` — the repo's shell-script conventions: `set -uo pipefail`, resolving `REPO_ROOT` from `BASH_SOURCE` so the script is caller-location-independent
    - `scripts/check_component_boundary.sh` — the loud-refusal idiom to reuse for every verification step (fail with a named reason, never pass vacuously)
  </read_first>
  <action>
    Write `deploy/01-cluster-up.sh` and `deploy/02-kfp-install.sh` following the repository's existing shell conventions: `set -uo pipefail`, `REPO_ROOT` resolved from `BASH_SOURCE` and `cd`'d into, and every check failing loudly with a named reason rather than passing silently.

    `deploy/01-cluster-up.sh` installs k3d if it is not already on `PATH` and creates the cluster. Install it to a user-writable directory without sudo: download the upstream installer to a file first, read it, then execute it, rather than piping a network response straight into a shell — the installer honours a no-sudo flag and an install-directory override, so confirm the exact variable names by reading the downloaded script instead of assuming them. Pin the release to v5.9.0. Then create a cluster named `mlops` with one server, zero agents, an advisory server memory budget of 10Gi, and `--wait`. Add a comment recording that k3d's memory flags are advisory rather than an enforced cgroup cap, so the real protection is the ParallelFor cap and the per-task resource limits already compiled into the DAG. Do not create a local registry: component images come from public GHCR, so a local registry is one more moving part with no purpose here. Finish by verifying `kubectl get nodes` reports the node Ready.

    `deploy/02-kfp-install.sh` installs KFP standalone at `PIPELINE_VERSION=2.17.0`: apply the `cluster-scoped-resources` kustomization at that ref, wait for the application CRD to be established, apply the **`env/platform-agnostic`** kustomization at the same ref, and wait for the `ml-pipeline` pods to be ready with a timeout generous enough for first-time image pulls. Use `env/platform-agnostic`, never `env/dev`: at the 2.17.0 tag, the `env/dev` overlay carries an image override block that repoints every KFP component image to a floating branch tag, so despite the pinned `?ref=` you would end up running whatever is on upstream's default branch at pull time (`03-RESEARCH.md` Pitfall 9).

    Then a verification block that is the script's real product, each check failing loudly:
    - every pod in the `kubeflow` namespace is Running or Completed, with the count printed;
    - every `ghcr.io/kubeflow/kfp-*` image running in the cluster carries the `2.17.0` tag — collect the images with a `kubectl get pods -o jsonpath` over container images, and fail naming any image that does not;
    - `kubectl get namespace istio-system` reports NotFound, which together with the pod check is REQ-B1's acceptance criterion;
    - the check set is non-vacuous: if the image list comes back empty, fail rather than report success, exactly as `check_component_boundary.sh` refuses an empty scan set.
  </action>
  <verify>
    <automated>bash deploy/01-cluster-up.sh &amp;&amp; bash deploy/02-kfp-install.sh</automated>
  </verify>
  <acceptance_criteria>
    - `kubectl get pods -n kubeflow --no-headers | grep -cvE ' (Running|Completed) '` is 0, and the total pod count is at least 5.
    - `kubectl get pods -n kubeflow -o jsonpath='{.items[*].spec.containers[*].image}' | tr ' ' '\n' | grep 'ghcr.io/kubeflow/kfp' | grep -vc ':2.17.0'` is 0, and the same pipeline without the final `grep -v` yields a non-zero count — the tag check is non-vacuous.
    - `kubectl get namespace istio-system` exits non-zero with a NotFound message.
    - `grep -c 'platform-agnostic' deploy/02-kfp-install.sh` is at least 1 and `grep -c 'env/dev' deploy/02-kfp-install.sh | grep -v '^#'` finds the overlay named only in a comment explaining why it was rejected, never in an applied command.
    - `kubectl port-forward -n kubeflow svc/ml-pipeline-ui 8080:80` followed by `node -e "fetch('http://localhost:8080').then(r=>console.log(r.status))"` prints 200.
    - Re-running `bash deploy/02-kfp-install.sh` against the already-installed cluster exits 0 rather than erroring — the script is safe to re-run.
  </acceptance_criteria>
  <done>A k3d cluster named `mlops` is running KFP standalone 2.17.0 from the platform-agnostic overlay, every KFP image is verified at the pinned tag, no Istio namespace exists, and the KFP UI answers over a port-forward.</done>
</task>

<task type="auto">
  <name>Task 2: Dedicated MinIO, in-cluster MLflow, credentials, and the raw-data upload</name>
  <precondition>`kubectl get pods -n kubeflow -l app=ml-pipeline` reports at least one Ready pod (Task 1's cluster is up), and `ls data/tlc/yellow_tripdata_*.parquet` lists at least the three months spanning 2020-02 through 2020-04 (Phase 2's `scripts/download_tlc_data.py` populated the gitignored local cache).</precondition>
  <reversibility rating="reversible">D-12's in-cluster topology can be swapped for the researched host-process MLflow fallback by changing the tracking URI the pipeline is submitted with; the client code in `lib/tracking.py` is identical either way, so this is a deployment change, not a code change.</reversibility>
  <files>deploy/03-storage-tracking.sh, deploy/minio-values.yaml, deploy/mlflow-values.yaml, deploy/04-upload-raw-data.sh</files>
  <read_first>
    - `.planning/phases/03-kubeflow-pipeline-core-deployment/03-RESEARCH.md` — the Code Examples section's MLflow and MinIO Helm install commands (the content source for both values files), Pitfall 10 (the MinIO chart's default memory request), and Open Questions 1 and 2
    - `.planning/research/PITFALLS.md` — Pitfall 7 (the MinIO/MLflow storage-collision trap, and in-cluster DNS versus a host-forwarded address)
    - `.planning/research/STACK.md` — the Installation section's MinIO and MLflow guidance and the `mlflow==3.15.1` pin
    - `pipelines/train_pipeline.py` — the exact Secret name and key names `_secret` expects, and the `mlflow_tracking_uri` / `s3_endpoint_url` defaults the deployed services must actually answer on
    - `lib/artifacts.py` — `RAW_BUCKET`, `BACKFILL_BUCKET`, and the object name `ingest_month_to_parquet` downloads
    - `lib/ingest.py` — `month_parquet_path`, for the exact `yellow_tripdata_{month}.parquet` object name the upload must produce
    - `scripts/download_tlc_data.py` — the existing host-side script conventions to mirror in the two new Python scripts
  </read_first>
  <action>
    Write `deploy/03-storage-tracking.sh` plus the two values files, then `deploy/04-upload-raw-data.sh`.

    Install Helm first if it is not on `PATH`, by the same download-read-execute pattern Task 1 uses for k3d, into the same user-writable directory without sudo.

    Credentials. Generate a MinIO root user and password with a cryptographic random source unless `MINIO_ROOT_USER` and `MINIO_ROOT_PASSWORD` are already exported, then create two Kubernetes Secrets and pass credentials to Helm only by Secret reference, never as a `--set` argument and never echoed to stdout:
    - `minio-root-creds` in the `mlflow` namespace, holding the chart's expected root-user and root-password keys, referenced from `deploy/minio-values.yaml` through the chart's existing-secret value;
    - `mlflow-minio-creds` holding `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY`, created in **both** the `mlflow` and `kubeflow` namespaces. Both copies are required: MLflow's server pod reads it in `mlflow`, and KFP standalone runs pipeline task pods in `kubeflow`, where `use_secret_as_env` resolves the Secret in the task pod's own namespace. A Secret present in only one namespace produces a confusing mid-run credential failure rather than an install-time error, so verify both copies exist before continuing.

    MinIO. Add the `https://charts.min.io/` repository and install the chart into the `mlflow` namespace with `deploy/minio-values.yaml`: standalone mode, a single replica, an **explicit** small memory request such as 512Mi, persistence enabled with a small volume, the existing-secret reference, and the three buckets `tlc-raw`, `mlflow-artifacts` and `backfill` provisioned by the chart's bucket-creation values. The explicit memory request is not optional — the chart's default is sized for a dedicated node and will leave the pod Pending with an insufficient-memory event on this machine (`03-RESEARCH.md` Pitfall 10). Persistence is enabled deliberately, diverging from the chart's own toy-setup example: the champion/candidate promotion story needs model artifacts to survive across separate pipeline runs, not merely within one.

    MLflow. Resolve the published chart tag with `helm show chart oci://ghcr.io/mlflow/charts/mlflow` and pin whatever it reports rather than installing an unpinned chart. Install into the `mlflow` namespace with `deploy/mlflow-values.yaml`: storage enabled with a small PVC for the SQLite file, a SQLite backend store URI on that mount, a default artifact root pointing at the `mlflow-artifacts` bucket, explicit CPU and memory requests and limits (the chart defaults to none), and an environment list setting the S3 endpoint to the **in-cluster** MinIO service DNS name plus the two AWS credential variables sourced from `mlflow-minio-creds` by secret reference. Never use a host-forwarded or loopback address for anything a pod resolves — that address only works from the host, and the failure appears only once code runs inside a pod (PITFALLS.md Pitfall 7). Immediately after install, check the MLflow pod's logs for a SQLite permission error; if the server cannot open the database file on the PVC, add a `podSecurityContext` group override to the values file (`03-RESEARCH.md` Open Question 2 flags this as a first-minutes check, not an upfront blocker).

    Verification block, each check failing loudly: both pods Ready in the `mlflow` namespace; all three buckets present; the MLflow HTTP endpoint answering from **inside** the cluster, tested with a throwaway pod rather than from the host, since in-cluster reachability is what the pipeline actually depends on; and both copies of `mlflow-minio-creds` present.

    `deploy/04-upload-raw-data.sh` uploads the locally cached TLC months into `tlc-raw` so component pods can read them. Port-forward the MinIO service, then use the repository's own `uv run` environment and boto3 to upload every `data/tlc/yellow_tripdata_*.parquet` file under the identical object name, because `lib.artifacts.ingest_month_to_parquet` downloads by exactly that name into a directory it then hands to `lib.ingest.month_parquet_path`. Refuse loudly and exit non-zero if the local cache is empty. Print the object names and byte counts uploaded, never a credential.
  </action>
  <verify>
    <automated>bash deploy/03-storage-tracking.sh &amp;&amp; bash deploy/04-upload-raw-data.sh</automated>
  </verify>
  <acceptance_criteria>
    - `kubectl get pods -n mlflow --no-headers | grep -cvE ' (Running|Completed) '` is 0, with at least two pods listed.
    - `kubectl get secret mlflow-minio-creds -n mlflow` and `kubectl get secret mlflow-minio-creds -n kubeflow` both exit 0.
    - A throwaway in-cluster pod (`kubectl run --rm -it --restart=Never --image=curlimages/curl`) fetching `http://mlflow.mlflow.svc.cluster.local:5000/health` returns a success status — reachability is proven from inside the cluster, not from the host.
    - Listing buckets through a port-forward with boto3 returns all three of `tlc-raw`, `mlflow-artifacts`, `backfill`.
    - Listing `tlc-raw` returns at least three objects named `yellow_tripdata_2020-02.parquet`, `yellow_tripdata_2020-03.parquet`, `yellow_tripdata_2020-04.parquet`.
    - `git ls-files deploy/ | xargs grep -Eic '(AKIA[0-9A-Z]{16}|rootPassword: *[^ ]|AWS_SECRET_ACCESS_KEY: *[A-Za-z0-9])'` totals 0 — no credential value is tracked in git.
    - `grep -Ec 'localhost|127\.0\.0\.1' deploy/mlflow-values.yaml` is 0 — pod-resolved addresses are in-cluster DNS names only.
    - `kubectl logs -n mlflow deploy/mlflow --tail=50 | grep -ci 'unable to open database file'` is 0.
  </acceptance_criteria>
  <done>A dedicated MinIO pod and an in-cluster MLflow server (SQLite backend, MinIO artifact root) are Ready in the `mlflow` namespace, the three buckets exist, the credentials Secret exists in both namespaces with no credential in git, and the raw TLC months are uploaded under the object names the ingest adapter expects.</done>
</task>

<task type="auto">
  <name>Task 3: Submit tooling and the first real per-month branch on the cluster</name>
  <precondition>`kubectl get pods -n mlflow` and `kubectl get pods -n kubeflow` both report Ready pods (Tasks 1 and 2 complete), and `docker pull ghcr.io/tothehien/nyc-trip-duration-kfp/ingest:$(git rev-parse HEAD)` succeeds, meaning CI has published images for the commit under test.</precondition>
  <files>scripts/submit_pipeline.py, scripts/backfill_checksums.py</files>
  <read_first>
    - `scripts/download_tlc_data.py` — the existing host-side script conventions: argparse surface, logging, and the `raise SystemExit(main())` entrypoint idiom
    - `pipelines/compile.py` and `pipelines/train_pipeline.py` — the compile entrypoint and the full pipeline parameter set to expose as submit arguments
    - `components/__init__.py` — `IMAGE_TAG` and how `COMPONENT_IMAGE_TAG` drives it
    - `lib/artifacts.py` — `artifact_key`, `BACKFILL_BUCKET`, so the checksum script addresses exactly the keys the features stage publishes
    - `lib/months.py` — `month_range`, for expanding a range host-side in the checksum script
    - `.planning/research/PITFALLS.md` — Pitfall 3 (cluster-side image pull) and Pitfall 2 (verify resource limits landed on the pod, do not trust a clean compile)
  </read_first>
  <action>
    Write `scripts/submit_pipeline.py`: an argparse CLI exposing `--start-month`, `--end-month`, `--features-version`, `--model-name`, `--experiment`, `--image-tag` (defaulting to `git rev-parse HEAD`), `--kfp-host` (defaulting to the port-forwarded KFP endpoint), and `--no-cache`, which submits the run with KFP's per-run caching disabled. The `--no-cache` flag is what plan 03-05's REQ-B8 proof needs: re-submitting identical parameters would otherwise cache-hit every task and never re-write the deterministic keys the byte-identity comparison depends on. It sets `COMPONENT_IMAGE_TAG` from `--image-tag` **before** importing the pipeline module, because the image references are bound at import time; compiles the package through the same `pipelines.compile` entrypoint CI uses, so a submitted run and a released YAML cannot diverge; then submits through the KFP client with the pipeline arguments, and prints the run id and the run URL. Refuse loudly if the resolved image tag is the unpublished placeholder rather than a real SHA — submitting the placeholder would produce a confusing image-pull failure several minutes into a run.

    Write `scripts/backfill_checksums.py`: an argparse CLI taking `--start-month`, `--end-month`, `--features-version`, `--endpoint-url` and an optional `--out`, which expands the range with `lib.months.month_range`, addresses each object at `lib.artifacts.artifact_key(month, "features", version)` in the `backfill` bucket, downloads each into memory, and prints a table of month, key, byte size and sha256. `03-RESEARCH.md` verified empirically against this repository's pinned pandas, pyarrow and lightgbm that no normalisation or metadata-stripping step is needed, so hash the bytes directly — do not build a Parquet-normalising layer to solve a problem that does not exist here. Refuse loudly if any expected key is missing rather than printing a partial table, since a partial table would silently understate a failed comparison. `--out` writes the same table as a markdown table for pasting into README.

    Then submit the first real run: port-forward the KFP endpoint and run `scripts/submit_pipeline.py --start-month 2020-02 --end-month 2020-04`. This task's required proof is the per-month branch, which is `research/ARCHITECTURE.md`'s build-order step 7 and D-10's safe-cut-line deliverable: for every month in the range, the `ingest`, `validate` and `features` tasks succeed, their pods pulled their GHCR images, and a deterministic backfill key exists. If the run continues past `merge` into `train`, `evaluate` and `register`, that is welcome, but plan 03-05 owns proving the tail; do not chase tail failures here beyond recording them in the SUMMARY.

    While the run is executing, capture the two things a clean compile cannot prove. First, that resource limits actually landed: inspect a running task pod with `kubectl get pod -o yaml` and confirm a real `resources` block with memory requests and limits is present, since a KFP SDK/backend mismatch silently drops them (PITFALLS.md Pitfall 2) — the matched 2.17.0 pair should prevent it, and this check is what turns "should" into evidence. Second, that at most `PARALLELISM` per-month branch pods are Running concurrently, which is REQ-B5's cap observed rather than merely compiled. Record both observations, plus the run id, in the SUMMARY for plan 03-05 to cite.
  </action>
  <verify>
    <automated>UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml --extra pipeline python scripts/submit_pipeline.py --start-month 2020-02 --end-month 2020-04 --image-tag "$(git rev-parse HEAD)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/submit_pipeline.py --image-tag dev` exits non-zero with a message naming the placeholder tag — the guard against submitting an unpublished reference has teeth.
    - The submitted run's `expand_months` task succeeds and its log lists exactly `2020-02`, `2020-03`, `2020-04`.
    - For all three months, the `ingest`, `validate` and `features` tasks report Succeeded in the KFP run view, and `kubectl get events -n kubeflow` contains no `ImagePullBackOff` or `ErrImagePull` for any `ghcr.io/tothehien/nyc-trip-duration-kfp/` image.
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra ml --extra pipeline python scripts/backfill_checksums.py --start-month 2020-02 --end-month 2020-04 --features-version v1` prints three rows with non-zero byte sizes and 64-character hex digests, and exits 0.
    - Deleting one object from the `backfill` bucket and re-running the checksum script makes it exit non-zero naming the missing key (restore it by re-running the pipeline afterwards) — the refuse-on-missing guard has teeth.
    - `kubectl get pod <a task pod> -n kubeflow -o jsonpath='{.spec.containers[*].resources}'` prints a non-empty object containing both `requests` and `limits` memory entries.
    - Sampling `kubectl get pods -n kubeflow` repeatedly during the ParallelFor stage never shows more than 2 per-month branch pods in Running state simultaneously.
  </acceptance_criteria>
  <done>The cluster runs a real submitted pipeline whose per-month branch completes for all three months with GHCR-pulled images, verified resource limits and an observed concurrency cap, and deterministic backfill keys exist in MinIO with reproducible checksums.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> host toolchain | The k3d and Helm installers are downloaded and executed on the developer machine |
| upstream manifests/charts -> cluster | KFP kustomize manifests and the MinIO and MLflow Helm charts run with cluster-scoped permissions |
| GHCR -> cluster | Task pods pull and execute component images |
| host shell -> Kubernetes Secrets | Generated credentials pass through a shell before landing in the cluster |
| pipeline task pod -> MinIO / MLflow | Component code authenticates across the cluster network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering | k3d and Helm installer scripts | high | mitigate | Both installers are downloaded to a file, read, and only then executed, rather than piped from a network response straight into a shell; both are pinned to a named release rather than a moving default |
| T-03-02 | Tampering | KFP backend images | high | mitigate | The `env/platform-agnostic` overlay is applied at `ref=2.17.0` so the base manifests' pinned tags survive; `deploy/02-kfp-install.sh` then verifies every running KFP image carries `2.17.0` and fails loudly on any floating tag, with a non-vacuity check so an empty image list cannot pass (03-RESEARCH.md Pitfall 9) |
| T-03-01 | Information Disclosure | credential generation and Helm invocation | high | mitigate | Credentials are generated from a cryptographic random source or read from the environment, passed to Helm only by Kubernetes Secret reference and never as a `--set` argument or on stdout, and never written to a tracked file; Task 2's acceptance criteria include a negative grep across `git ls-files deploy/` |
| T-03-12 | Spoofing | MinIO/MLflow endpoint resolution inside pods | medium | mitigate | Every pod-resolved address is an in-cluster service DNS name; loopback and host-forwarded addresses appear only in host-side tooling, asserted by a negative grep over `deploy/mlflow-values.yaml` (PITFALLS.md Pitfall 7) |
| T-03-04 | Denial of Service | MinIO chart default memory request, unbounded fan-out | medium | mitigate | The MinIO memory request is set explicitly to a small value rather than inheriting the chart's node-sized default (Pitfall 10); the k3d memory budget is documented as advisory, with the real protection being the compiled ParallelFor cap and per-task limits, both observed on live pods in Task 3 |
| T-03-09 | Elevation of Privilege | cluster-scoped RBAC created by the KFP manifests | low | accept | The manifests are upstream-managed and applied unmodified at a pinned tag on a single-node local cluster with no ingress and no second tenant; auditing upstream's RBAC is out of scope for this milestone |
| T-03-06 | Information Disclosure | public GHCR packages, no image pull secrets | low | accept | Deliberate portfolio tradeoff already documented in README (D-05); it is also what makes the cluster-side pull work with zero registry-credential wiring |
</threat_model>

<verification>
- `kubectl get pods -n kubeflow` all Running, `kubectl get namespace istio-system` NotFound, every KFP image at `2.17.0`.
- `kubectl get pods -n mlflow` all Running, three buckets present, MLflow reachable from inside the cluster.
- `mlflow-minio-creds` present in both namespaces; no credential value in any tracked file.
- A submitted run's per-month branch succeeds for all three months with no image-pull failure.
- Live pods carry real memory requests and limits, and no more than two branch pods run concurrently.
- The checksum script prints three digests and refuses loudly on a missing key.
</verification>

<success_criteria>
- REQ-B1 satisfied and verified mechanically, including the Pitfall 9 tag check that distinguishes a genuinely pinned install from a floating one.
- The D-12 topology runs: dedicated MinIO pod, in-cluster MLflow on SQLite, artifacts in a dedicated bucket, credentials only in Secrets.
- REQ-B3's cluster half proven: pods pull the CI-built GHCR images successfully, which a `docker push` alone never demonstrates.
- The safe-cut-line deliverable exists: at least one full per-month branch proven end-to-end on the cluster.
- Host-side submit and checksum tooling exists for plan 03-05's proofs, each refusing loudly rather than reporting partial success.
</success_criteria>

<output>
Create `.planning/phases/03-kubeflow-pipeline-core-deployment/03-04-SUMMARY.md` when done.
</output>
