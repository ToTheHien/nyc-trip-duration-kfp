---
phase: 03-kubeflow-pipeline-core-deployment
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - README.md
  - tests/test_readme.py
  - .planning/PROJECT.md
autonomous: true
requirements: [REQ-E1, REQ-E2, REQ-E3]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README contains an `## Architecture` section carrying a rendered diagram (a fenced mermaid block), not prose alone, covering the full expand_months -> ingest -> validate -> features -> merge -> train -> evaluate -> register flow plus the notify exit path (REQ-E1)."
    - "The architecture section states the artifact-store / registry split explicitly: KFP's pipeline_root holds run-scoped intermediate bytes, MLflow plus its dedicated MinIO bucket holds the durable cross-run champion record."
    - "README contains an `## ADRs` section with one entry per recorded decision, covering at minimum the four PROJECT.md Key Decisions plus D-10, D-11, D-12 and D-13 (REQ-E2)."
    - "README contains an explicit `## Next Steps` section naming the deferred scope — KServe serving, the dashboard, and recurring/cron runs among others — rather than being silent about it (REQ-E3)."
    - "README contains a `## Cluster Deployment` section naming the four `deploy/` scripts in execution order and the verification command for each."
    - "`tests/test_readme.py` fails if a later edit strips or empties any of the four new sections, so re-running `scripts/qa.sh test` after any README change re-checks them (the sections cannot silently rot)."
    - "No README section, code block or example contains a real credential, access key, or a tracking URI with embedded authentication."
  prohibitions:
    - statement: "MUST NOT describe a capability in README's architecture or ADR sections that the repository does not actually implement at the commit README is written on. A portfolio README that documents an aspirational DAG is worse than one that documents a smaller real one."
      category: transparency
  artifacts:
    - path: "README.md"
      provides: "Architecture diagram, cluster deployment runbook, ADR set, and deferred-scope statement"
      contains: "## Architecture"
    - path: "tests/test_readme.py"
      provides: "Standing regression tests for the four new README sections, reusing the existing _section_body helper"
  key_links:
    - from: "tests/test_readme.py"
      to: "README.md"
      via: "_section_body lookups for the four new headings"
      pattern: "_section_body\\(text, \"## "
---

<objective>
Write the documentation half of Phase 3: the architecture diagram, the cluster deployment runbook, the ADR set covering every recorded decision, and the explicit deferred-scope statement — each guarded by a standing regression test so a later edit cannot silently strip it.

Purpose: REQ-E1/E2/E3 are cluster-independent. Landing them in the same wave as the cluster bring-up means the documentation deliverable survives intact even if D-10's cluster time-box is blown and the phase falls back to `research/ARCHITECTURE.md`'s safe cut line.
Output: four new README sections and their paired tests, plus PROJECT.md's Key Decisions outcomes filled in.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-kubeflow-pipeline-core-deployment/03-CONTEXT.md
@.planning/phases/03-kubeflow-pipeline-core-deployment/03-RESEARCH.md
@.planning/phases/03-kubeflow-pipeline-core-deployment/03-02-SUMMARY.md
@.planning/research/ARCHITECTURE.md
</context>

## Artifacts this phase produces

See `03-01-PLAN.md`'s inventory. This plan creates the README sections `## Architecture`, `## Cluster Deployment`, `## ADRs` and `## Next Steps`, and the test functions guarding them in `tests/test_readme.py`.

Note on heading collision: README already has `## Architectural Contract: Thin Component, Fat Lib`. `## Architecture` is not a prefix of it (`Architectur` + `e` versus `Architectur` + `al`), so `tests/test_readme.py`'s substring-based `_section_body` helper resolves them distinctly — but assert that explicitly in the new test so a future rename cannot make the two sections alias each other.

<tasks>

<task type="auto">
  <name>Task 1: Architecture section with a rendered diagram, and the cluster deployment runbook</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — the whole file: existing heading set, the `## Architectural Contract: Thin Component, Fat Lib` section this one complements, and the prose voice of `## Feature Engineering Benchmark` / `## Dataset and Drift Window` to match
    - `pipelines/train_pipeline.py` — the actual compiled DAG being documented; the diagram must match this file, not the plan
    - `components/` — the nine directory names, so the diagram's node labels match the real image names
    - `.planning/research/ARCHITECTURE.md` — the System Overview diagram, the Training Run Flow, and the Artifact Store / Registry Split block, which are the content source
    - `.planning/phases/03-kubeflow-pipeline-core-deployment/03-RESEARCH.md` — the MLflow and MinIO Helm install commands and the KFP overlay commands, which are the content source for the runbook
    - `.planning/research/PITFALLS.md` — Pitfall 9 (verify the installed KFP image tags), Pitfall 10 (the MinIO chart's default memory request)
  </read_first>
  <action>
    Add `## Architecture` to README, placed after `## Architectural Contract: Thin Component, Fat Lib` so the contract statement leads into the concrete DAG. It contains:

    - A fenced ```mermaid flowchart whose nodes are the nine real stage names (`expand_months`, `ingest`, `validate`, `features`, `merge`, `train`, `evaluate`, `register`, `notify`), showing the ParallelFor fan-out over months, the `dsl.Collected` fan-in into `merge`, the `dsl.If` gate on `register`, and `notify` attached as the ExitHandler exit path covering both success and failure. Read the node names out of `pipelines/train_pipeline.py` rather than from this plan, so the diagram documents the code that exists.
    - A short prose paragraph naming the three tiers: `lib/` holds 100% of the pandas, pandera, LightGBM and MLflow logic; `components/` holds nine thin wrappers that read a typed artifact path, make one `lib` call, and write a typed artifact path; `pipelines/` holds only control flow. Point at `scripts/check_component_boundary.sh` as the mechanical enforcement.
    - An explicit statement of the artifact-store / registry split, sourced from `research/ARCHITECTURE.md`: KFP's `pipeline_root` holds run-scoped intermediate bytes and drives the lineage graph, while MLflow plus its dedicated MinIO bucket holds the durable, queryable-across-runs champion record that makes promotion decisions comparable between separate executions. State that `evaluate` reads the champion RMSE from MLflow's `@champion` alias, never from a prior KFP run's artifacts.
    - A note that a third bucket, `backfill`, holds the deterministic per-month feature keys REQ-B8's idempotency proof diffs, and that this is deliberately separate from KFP's run-scoped artifact path because a run-ID-scoped URI cannot be diffed across runs (`research/PITFALLS.md` Pitfall 5).

    Add `## Cluster Deployment` after it — the runbook a reviewer follows to reproduce the stack. It names the four `deploy/` scripts in execution order (`01-cluster-up.sh`, `02-kfp-install.sh`, `03-storage-tracking.sh`, `04-upload-raw-data.sh`), states what each one does in one line, and gives the verification command for each: pods ready in the `kubeflow` namespace, every `ghcr.io/kubeflow/kfp-*` image resolving to the pinned `2.17.0` tag rather than a floating branch tag, the absence of an Istio namespace, the MinIO and MLflow pods ready in the `mlflow` namespace, and the three buckets present. Then document submitting a run with `scripts/submit_pipeline.py`. Record two facts a reader will otherwise trip over: the KFP standalone install uses the `env/platform-agnostic` kustomize overlay rather than the `env/dev` overlay the current kubeflow.org quick start recommends, because `env/dev` repoints every component image to a floating branch tag and pulls in cloud-provider resources irrelevant to k3d (`03-RESEARCH.md` Pitfall 9); and the MinIO chart's memory request must be set explicitly because its default is sized for a dedicated node and will not schedule on a 16GB laptop (`03-RESEARCH.md` Pitfall 10).

    Credentials are generated by the deploy scripts and stored as Kubernetes Secrets. README states how to read them back with `kubectl get secret` when a human needs the MinIO or MLflow UI, and contains no credential value, no access key and no tracking URI with embedded authentication anywhere.
  </action>
  <verify>
    <automated>grep -q '^## Architecture$' README.md &amp;&amp; grep -q '^## Cluster Deployment$' README.md &amp;&amp; grep -q '```mermaid' README.md &amp;&amp; scripts/qa.sh test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Architecture$' README.md` is 1, and `grep -c '^## Architectural Contract' README.md` is still 1 — the two headings coexist distinctly.
    - The `## Architecture` section body contains a fenced mermaid block, and every one of the nine stage names (`expand_months`, `ingest`, `validate`, `features`, `merge`, `train`, `evaluate`, `register`, `notify`) appears inside it.
    - `grep -c 'platform-agnostic' README.md` is at least 1 and `grep -c 'env/dev' README.md` is at least 1 — the overlay choice and the rejected alternative are both recorded.
    - `grep -Eic '(AKIA[0-9A-Z]{16}|aws_secret_access_key *[:=] *[^ ]|://[^ /]*:[^ /]*@)' README.md` is 0 — no credential and no URI with embedded auth.
    - Every `deploy/` script named in the runbook exists on disk once plan 03-04 lands; run `for s in 01-cluster-up 02-kfp-install 03-storage-tracking 04-upload-raw-data; do grep -q "$s" README.md || echo "MISSING $s"; done` and confirm it prints nothing.
  </acceptance_criteria>
  <done>README documents the real DAG with a rendered diagram and the artifact-store/registry split, and carries a reproducible cluster runbook naming every deploy script and its verification command.</done>
</task>

<task type="auto">
  <name>Task 2: ADR set and the deferred-scope statement</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — the sections written in Task 1, and the existing `## Repository and Package Visibility (D-05)` section, whose shape the ADR entries should echo
    - `.planning/PROJECT.md` — the Key Decisions table: the four decisions and their recorded rationales
    - `.planning/phases/03-kubeflow-pipeline-core-deployment/03-CONTEXT.md` — D-10, D-11, D-12, D-13 with their rationales and reversibility notes
    - `.planning/phases/03-kubeflow-pipeline-core-deployment/03-02-PLAN.md` — the two design decisions locked there (`dsl.If` without `dsl.OneOf`, and the compile-time parallelism constant)
    - `.planning/phases/03-kubeflow-pipeline-core-deployment/03-01-PLAN.md` — the five design decisions locked there, notably the uniform component style and the deterministic key format
    - `.planning/REQUIREMENTS.md` — the `## Out of Scope (v1)` list, which is the content source for Next Steps
  </read_first>
  <action>
    Add `## ADRs` to README. Each entry is a `### ADR-NNN: <short title>` heading followed by three labelled paragraphs — Context, Decision, Consequences — in the compact style of the existing D-05 section. Write at minimum these ten, in this order:

    - ADR-001: cut the original four-phase plan to three phases for the 10-15h/1-week budget on a 16GB laptop (PROJECT.md Key Decision 1).
    - ADR-002: trip-duration regression rather than tip-percentage classification (Key Decision 2).
    - ADR-003: a 12-month drift-spanning window rather than the full 2009-present TLC history (Key Decision 3), cross-referencing the existing `## Dataset and Drift Window` section rather than restating its numbers.
    - ADR-004: KFP standalone on k3d rather than the full multi-user Kubeflow/Istio distribution (Key Decision 4).
    - ADR-005: D-10 — attempt the cluster install directly under a firm time-box with no dedicated spike, with `research/ARCHITECTURE.md`'s safe cut line pre-specified as the fallback. Consequences state what the fallback deliverable is.
    - ADR-006: D-11 — the ExitHandler notify task is one structured log line and nothing more. Consequences must record the load-bearing constraint: the exit task is constructed before the handler's `with` block and therefore cannot consume any output produced inside it, so the run's RMSE provably cannot appear in that line (`03-RESEARCH.md` Pitfall 11, `kubeflow/pipelines#10187`).
    - ADR-007: D-12 — MLflow deployed in-cluster with a dedicated MinIO pod and a SQLite backend store, not the host-process fallback and not KFP's own bundled object store. Consequences name the documented escape hatch if RAM pressure forces it.
    - ADR-008: D-13 — the byte-identical backfill proof runs over a 2-3 month subset rather than the full 12-month window, and why a subset is equally rigorous. Consequences record the one real precondition: both runs must use the same component image, because a rebuilt image with different pinned library versions changes the Parquet footer metadata even when the data is identical.
    - ADR-009: `dsl.If` without `dsl.OneOf`, with the reasoning from `03-02-PLAN.md`'s design decision 1 — the only end-of-run consumer is the exit task, and it cannot consume in-scope outputs, so a both-branch collection channel would have no consumer.
    - ADR-010: uniform `@dsl.component(base_image=...)` components over container components, and the `env/platform-agnostic` KFP overlay over `env/dev`. Consequences record that every component image bakes in `kfp` so the function-component bootstrap resolves locally rather than reaching PyPI at pod start, and that the overlay choice is what keeps the backend pinned to 2.17.0.

    Add `## Next Steps` last, an explicit bulleted deferred-scope statement rather than silence (REQ-E3). Source it from `REQUIREMENTS.md`'s `## Out of Scope (v1)` list — KServe serving, ONNX export, request batching, k6 load testing, recurring/cron pipeline runs, the Streamlit/React dashboard, full 2009-present ingestion, the multi-user Kubeflow/Istio distribution, hyperparameter tuning, tip-percentage classification, a feature store, distributed training, and external alerting integrations. Each bullet says in a clause why it was deferred, not merely that it was. Add a final bullet for any P2 requirement that ended up unimplemented, or state explicitly that every P2 requirement landed — REQUIREMENTS.md's Definition of Done requires unimplemented P2 items to be logged here rather than silently dropped.

    Every ADR must describe a decision the repository actually implements at this commit. If any entry would describe something aspirational, cut the entry rather than the truth.
  </action>
  <verify>
    <automated>grep -q '^## ADRs$' README.md &amp;&amp; grep -q '^## Next Steps$' README.md &amp;&amp; test "$(grep -c '^### ADR-' README.md)" -ge 10 &amp;&amp; scripts/qa.sh test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### ADR-' README.md` is at least 10, and `grep -c '^### ADR-00[1-4]:' README.md` is 4 — the four PROJECT.md Key Decisions each have an entry.
    - The `## ADRs` section body mentions each of `D-10`, `D-11`, `D-12`, `D-13`.
    - The ADR-006 entry mentions the constraint that the exit task cannot consume outputs produced inside the handler's scope.
    - The ADR-008 entry mentions the same-component-image precondition for the byte-identity claim.
    - The `## Next Steps` section body contains at least 10 bullet lines and mentions `KServe`, `dashboard`, and `recurring`.
    - `grep -Ec 'Context|Decision|Consequences' README.md` shows all three labels present in the ADR section for every entry (spot-check three entries by eye).
  </acceptance_criteria>
  <done>README carries at least ten ADR entries covering every recorded decision including the four PROJECT.md Key Decisions and D-10 through D-13, and an explicit deferred-scope statement.</done>
</task>

<task type="auto">
  <name>Task 3: Standing regression tests for the new sections, and PROJECT.md decision outcomes</name>
  <files>tests/test_readme.py, .planning/PROJECT.md</files>
  <read_first>
    - `tests/test_readme.py` — the whole file: the module docstring's stated purpose, the `_section_body` helper, and the two existing test functions whose assertion shape the new ones must clone
    - `README.md` — the four sections written in Tasks 1 and 2
    - `.planning/PROJECT.md` — the Key Decisions table, whose Outcome column currently reads pending for all four rows
  </read_first>
  <action>
    Extend `tests/test_readme.py` with four new test functions, reusing the existing `_section_body` helper — do not write a second helper. Extend the module docstring to record that REQ-E1/E2/E3 join REQ-C5/REQ-D1 as the sections these tests guard.

    - `test_architecture_section_contains_a_rendered_diagram` — asserts the `## Architecture` body contains a fenced mermaid block and that all nine stage names appear inside it. Derive the nine names by listing the `components/` directory rather than hardcoding a literal list, so adding or renaming a component makes the test speak up.
    - `test_architecture_heading_does_not_alias_the_contract_section` — asserts `_section_body(text, "## Architecture")` and `_section_body(text, "## Architectural Contract")` return different bodies, so a future rename cannot make the substring lookup silently resolve to the wrong section.
    - `test_adr_section_covers_every_recorded_decision` — asserts the `## ADRs` body contains at least ten `### ADR-` headings and mentions each of `D-10`, `D-11`, `D-12`, `D-13`.
    - `test_next_steps_section_names_deferred_scope` — asserts the `## Next Steps` body contains at least ten bullet lines and mentions KServe, the dashboard, and recurring runs.
    - `test_cluster_deployment_section_names_every_deploy_script` — asserts the `## Cluster Deployment` body names every executable file under `deploy/`, discovered by listing that directory rather than hardcoded, so a script added later without documentation fails the suite. Skip the assertion cleanly with a clear message if `deploy/` does not exist yet, since plan 03-04 creates it in the same wave.

    Then update `.planning/PROJECT.md`'s Key Decisions table: replace each row's pending Outcome with a one-clause outcome statement plus the matching `README.md` ADR identifier, so the decision record and its write-up point at each other. Change nothing else in that file.
  </action>
  <verify>
    <automated>scripts/qa.sh lint &amp;&amp; scripts/qa.sh format &amp;&amp; scripts/qa.sh test</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/qa.sh test` exits 0 with the four (or five) new `test_*` functions in `tests/test_readme.py` passing.
    - `grep -c '_section_body' tests/test_readme.py` counts one definition and at least six call sites — the helper was reused, not duplicated.
    - Temporarily deleting the `## Next Steps` section from README makes `scripts/qa.sh test` fail; restoring it makes it pass again — the guard has teeth.
    - `grep -Ec 'components/|listdir|iterdir|glob' tests/test_readme.py` is at least 1 — the nine stage names are discovered from the filesystem, not hardcoded.
    - `grep -c 'Pending' .planning/PROJECT.md` is 0 in the Key Decisions table region, and each of the four rows names an `ADR-00` identifier.
  </acceptance_criteria>
  <done>Every new README section is guarded by a standing regression test that fails when the section is stripped, and PROJECT.md's Key Decisions each point at their ADR write-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private cluster state -> public README | Deployment instructions and evidence written into a public repository could carry cluster secrets |
| documentation -> reviewer's mental model | A reader trusts README's description of what the repository does |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `README.md` cluster deployment runbook | high | mitigate | The runbook documents how to read credentials back from Kubernetes Secrets with `kubectl get secret`, never a credential value; Task 1's acceptance criteria include a negative grep for access-key patterns and for URIs carrying embedded authentication |
| T-03-06 | Information Disclosure | public GHCR packages, no image pull secrets | low | accept | Already disclosed in the existing `## Repository and Package Visibility (D-05)` section; the ADR set cross-references it rather than restating the tradeoff |
| T-03-10 | Repudiation | README describing capabilities the code does not implement | medium | mitigate | Every ADR must describe a decision implemented at this commit; the architecture diagram's node names are read out of `pipelines/train_pipeline.py` and re-derived from the `components/` directory by `tests/test_readme.py`, so documentation drift from the real DAG fails the suite |
</threat_model>

<verification>
- `scripts/qa.sh lint && scripts/qa.sh format && scripts/qa.sh test` all exit 0.
- README renders a mermaid diagram covering all nine real stage names.
- README carries at least ten ADR entries and an explicit deferred-scope statement.
- Stripping any of the four new sections fails the test suite.
- No credential pattern appears anywhere in README.
</verification>

<success_criteria>
- REQ-E1 satisfied by a rendered diagram, not prose, whose node names are derived from the real code.
- REQ-E2 satisfied by ADR entries for the four PROJECT.md Key Decisions plus D-10 through D-13 plus the two Phase 3 design decisions.
- REQ-E3 satisfied by an explicit Next Steps section that also logs any unimplemented P2 requirement rather than dropping it.
- Every new section is guarded by a test that fails when it is stripped.
- PROJECT.md's Key Decisions table and README's ADRs cross-reference each other.
</success_criteria>

<output>
Create `.planning/phases/03-kubeflow-pipeline-core-deployment/03-03-SUMMARY.md` when done.
</output>
