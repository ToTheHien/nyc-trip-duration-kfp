---
phase: 02-data-model-engineering-lib
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - scripts/qa.sh
  - .github/workflows/ci.yml
  - .gitignore
  - lib/ingest.py
  - lib/schemas.py
  - lib/features.py
  - lib/train.py
  - lib/evaluate.py
  - lib/registry.py
  - tests/lib/test_tracer_end_to_end.py
autonomous: false
requirements: [REQ-C1, REQ-C2, REQ-C3, REQ-C4, REQ-D1, REQ-D2, REQ-D3]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`scripts/qa.sh test` and `scripts/qa.sh typecheck` succeed on a tree containing a `lib/` module that imports pandas — i.e. the `ml` optional-dependency group is installed by the shared entrypoint, not only the `dev` group."
    - "A single end-to-end test drives one synthetic month of trip rows through every layer this phase owns (chunked Parquet read -> trip-duration derivation -> pandera validation -> zone-centroid join + vectorized haversine + dtype downcast -> LightGBM fit -> RMSE -> champion lookup against a mocked MLflow client) and asserts a finite, positive RMSE float."
    - "The six `lib/` modules created here expose the exact public symbol names listed in `## Artifacts this phase produces`, so plans 02-03, 02-04 and 02-05 can expand them in parallel without renegotiating interfaces."
    - "`scripts/qa.sh test` passes the pre-existing `--cov-fail-under=100` branch-coverage gate with all six new modules present."
    - "The REQ-D1 / D-07 backfill window is pinned in exactly one place (`lib.ingest.TLC_START_MONTH` / `TLC_END_MONTH` = `2019-07` / `2020-06`), enumerates to 12 months via `lib.months.month_range`, and contains `2020-03` so the window provably crosses the COVID drift event."
    - statement: "A reviewer can confirm `pyshp` and `pyproj` are legitimate PyPI packages before they are installed, because both were flagged `[SUS]` by the package-legitimacy checker and neither appeared in Phase 1's pinned STACK.md."
      verification: backstop
  artifacts:
    - pyproject.toml with a populated `ml` optional-dependency group
    - scripts/qa.sh with `--extra ml` in both UV_RUN branches
    - lib/ingest.py
    - lib/schemas.py
    - lib/features.py
    - lib/train.py
    - lib/evaluate.py
    - lib/registry.py
    - tests/lib/test_tracer_end_to_end.py
  key_links:
    - "scripts/qa.sh UV_RUN array -> every pandas-importing lib/ module (breaks with ModuleNotFoundError if the ml extra is missing)"
    - ".github/workflows/ci.yml `uv sync` -> scripts/qa.sh (CI must install the same extras the script runs against, or the pre-commit/CI parity claim in README.md breaks)"
    - "lib/features.py `build_features` -> lib/train.py `train_trip_duration_model` (feature frame column set and dtypes are the training contract)"
    - "lib/evaluate.py `beats_champion` -> lib/registry.py `ModelRegistry.get_champion_rmse` (the None-means-no-champion contract)"
  prohibitions:
    - statement: "No module under `lib/` may import `kfp` or `kfp_kubernetes` — Phase 2 code must stay cluster-free so Phase 3 can wrap it in thin components."
      status: active
      verification: explicit
    - statement: "The tracer end-to-end test must not be deleted, renamed away, or marked skipped by any later plan in this phase — it is the regression harness that keeps the expansion plans honest."
      status: active
      verification: explicit
    - statement: "The `ml` optional-dependency group must not absorb the shapefile/reprojection tooling used by the one-time centroid precompute; those belong to `dev` so no future component image ships them."
      status: active
      verification: explicit
---

<objective>
Make the repository able to hold pandas code at all, then prove the entire Phase 2 architecture end-to-end with one thin, production-quality vertical slice before any layer is built out horizontally.

Purpose: Phase 1 left `scripts/qa.sh` hardcoded to `--extra dev` and `pyproject.toml`'s `ml` group empty. The first `import pandas` under `lib/` fails the shared quality-gate entrypoint until both are fixed. Once fixed, a single end-to-end tracer through ingest -> schemas -> features -> train -> evaluate -> registry locks the six module interfaces so plans 02-03, 02-04 and 02-05 can expand them without renegotiating signatures.

Output: A populated `ml` dependency group, an `--extra ml`-aware `scripts/qa.sh` and CI workflow, six minimal-but-real `lib/` modules, and one end-to-end test producing a real RMSE number from a synthetic month.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-model-engineering-lib/02-CONTEXT.md
@.planning/phases/02-data-model-engineering-lib/02-RESEARCH.md
@.planning/phases/02-data-model-engineering-lib/02-PATTERNS.md
@CLAUDE.MD
@.claude/rules/requirements.md
</context>

## Artifacts this phase produces

Full inventory for the whole of Phase 2 (this plan creates the items marked **[02-01]**; later plans expand them).

**`lib/` modules and public symbols**

- `lib/ingest.py` **[02-01: `TLC_START_MONTH`, `TLC_END_MONTH`, `TLC_DATA_DIR`, `DEFAULT_BATCH_SIZE`, `month_parquet_path`, `read_month_chunked`, `add_trip_duration`]** — plus `FilterReport` (frozen dataclass: `total_rows`, `dropped_non_positive_distance`, `dropped_non_positive_duration`, `dropped_unmapped_zone`, `dropped_passenger_count`, `kept_rows`), `filter_trip_quality(df)`, `load_month(month, data_dir, batch_size)`
- `lib/schemas.py` **[02-01: `trip_schema`, `validate_trips`]** — `VALID_ZONE_MIN`, `VALID_ZONE_MAX`, `PLACEHOLDER_ZONE_IDS`, `PASSENGER_COUNT_MIN`, `PASSENGER_COUNT_MAX`, `trip_schema` (pandera `DataFrameSchema`), `validate_trips(df)`
- `lib/features.py` **[02-01: `EARTH_RADIUS_KM`, `ZONE_CENTROID_PATH`, `load_zone_centroids`, `haversine_km`, `build_features`, `FEATURE_COLUMNS`, `TARGET_COLUMN`]** — plus `ZONE_CATEGORY_DTYPE`, `haversine_km_rowwise(df)`, `join_zone_centroids(df, centroids)`, `downcast_features(df)`
- `lib/train.py` **[02-01: `LGBM_PARAMS`, `CATEGORICAL_FEATURES`, `train_trip_duration_model`]** — plus `SPLIT_TIMESTAMP`, `chronological_split(df, split_ts)`, `save_model(model, path)`, `load_booster(path)`
- `lib/evaluate.py` **[02-01: `rmse`]** — plus `EvaluationResult` (frozen dataclass: `rmse`, `n_rows`), `evaluate_model(model, x_test, y_test)`, `beats_champion(candidate_rmse, champion_rmse)`
- `lib/registry.py` **[02-01: `ModelRegistry`, `CHAMPION_ALIAS`, `RMSE_TAG`, `get_champion_rmse`]** — plus `CANDIDATE_ALIAS`, `tag_version_rmse(version, rmse)`, `set_candidate(version)`, `promote_to_champion(version)`

**Scripts (CLI)**

- `scripts/download_tlc_data.py` — flags `--start-month`, `--end-month`, `--dest`, `--force` (plan 02-02)
- `scripts/precompute_zone_centroids.py` — flags `--url`, `--out`, `--work-dir` (plan 02-02)
- `scripts/benchmark_features.py` — flags `--parquet`, `--rows`, `--out` (plan 02-05)

**Data / config files**

- `data/zone_centroids.csv` — committed, 263 rows, columns `zone_id`, `centroid_lat`, `centroid_lon` (plan 02-02)
- `data/tlc/yellow_tripdata_YYYY-MM.parquet` — gitignored local cache, 12 files (plan 02-02)

**Tests**

- `tests/lib/test_tracer_end_to_end.py` **[02-01]**, `tests/lib/test_train.py`, `tests/lib/test_evaluate.py`, `tests/lib/test_registry.py` (plan 02-03), `tests/lib/test_ingest.py`, `tests/lib/test_schemas.py` (plan 02-04), `tests/lib/test_features.py` (plan 02-05)

**Modified existing files**

- `pyproject.toml` **[02-01]**, `uv.lock` **[02-01]**, `scripts/qa.sh` **[02-01]**, `.github/workflows/ci.yml` **[02-01]**, `.gitignore` **[02-01]**, `README.md` (plan 02-05)

## Multi-Source Coverage Audit (whole plan set)

Plan roles: **02-01** foundation + tracer (wave 1) · **02-02** data acquisition scripts (wave 2) · **02-03** train / evaluate / registry (wave 2) · **02-04** ingest + schemas (wave 3) · **02-05** features + benchmark + README (wave 4).

**GOAL — ROADMAP Phase 2 success criteria**

| Item | Status | Plan |
|---|---|---|
| G-1 Chunked/streaming ingest over the pinned 12-month drift window | COVERED | 02-01 T3, 02-02 T1, 02-04 T1 |
| G-2 pandera schema with real Checks that fails loudly on a drifted month | COVERED | 02-01 T3, 02-04 T2 |
| G-3 Vectorized haversine + dtype downcast + README before/after benchmark table | COVERED | 02-01 T3, 02-05 T1/T2/T3 |
| G-4 Single fixed LightGBM config, no tuning framework anywhere | COVERED | 02-01 T3, 02-03 T2 |
| G-5 MLflow registry wrapper using alias promotion, unit-testable with a mocked client | COVERED | 02-01 T3, 02-03 T3 |

**REQ — phase requirement IDs**

| Item | Status | Plan |
|---|---|---|
| REQ-C1 pandera validation at ingest boundary | COVERED | 02-01, 02-04 |
| REQ-C2 Vectorized haversine | COVERED | 02-01, 02-02, 02-05 |
| REQ-C3 dtype downcasting | COVERED | 02-01, 02-05 |
| REQ-C4 Chunked reads | COVERED | 02-01, 02-04 |
| REQ-C5 Benchmark table in README | COVERED | 02-05 |
| REQ-D1 12-month TLC drift window | COVERED | 02-01 (window pinned), 02-02 (downloaded), 02-03 (split across the drift) |
| REQ-D2 LightGBM regression, no tuning | COVERED | 02-01, 02-03 |
| REQ-D3 MLflow champion/candidate aliasing | COVERED | 02-01, 02-03 |

**RESEARCH — 02-RESEARCH.md findings, pitfalls, security items**

| Item | Status | Plan |
|---|---|---|
| R-1 Pitfall 5: `scripts/qa.sh` only passes `--extra dev` | COVERED | 02-01 T2 |
| R-2 Pitfall 1: the classic S3 zone-shapefile URL is dead (403); use the CloudFront host | COVERED | 02-02 T1, T2 |
| R-3 Pitfall 2: shapefile is EPSG:2263 feet, needs reprojection to EPSG:4326 | COVERED | 02-02 T2 |
| R-4 Pitfall 3: zone IDs 264/265 are real but have no geometry | COVERED | 02-04 T1 |
| R-5 Pitfall 4: 23 of 263 zones are multi-part polygons | COVERED | 02-02 T2 |
| R-6 Pitfall 6: pandera per-row semantics vs. the ~1% baseline violation rate | COVERED | 02-04 T1, T2 |
| R-7 Pitfall 7: `congestion_surcharge`/`airport_fee` are legitimately all-null pre-2019-02 | COVERED | 02-04 T2 |
| R-8 Pitfall 8: `passenger_count`/`RatecodeID` are float64 and can hold NaN | COVERED | 02-05 T2 |
| R-9 Pitfall 9: some rows' pickup timestamps fall outside their file's nominal month | COVERED | 02-03 T1 |
| R-10 Pitfall 10: `--cov-fail-under=100` applies to every new module | COVERED | every plan's `<verify>` |
| R-11 Package legitimacy: `pyshp`/`pyproj` flagged `[SUS]`, need a checkpoint | COVERED | 02-01 T1 |
| R-12 LightGBM silently treats unseen categories as missing | COVERED | 02-03 T2 |
| R-13 `pyshp`/`pyproj` belong to `dev`, not `ml` | COVERED | 02-01 T2 |
| R-14 Security: zip-slip when extracting the zone shapefile archive | COVERED | 02-02 T2 |
| R-15 Security: prefer native LightGBM text serialization over pickle | COVERED | 02-03 T2 |
| R-16 Architectural Responsibility Map: the geo precompute lives in `scripts/`, never `lib/` | COVERED | 02-02 T2, 02-05 T1 |

**CONTEXT — locked decisions and discretion areas**

| Item | Status | Plan |
|---|---|---|
| D-06 Static zone-centroid lookup table joined on zone ID | COVERED | 02-02 T2, 02-05 T1 |
| D-07 Real full 12-month download; unit tests stay on tiny synthetic fixtures | COVERED | 02-02 T1; all test tasks |
| D-08 Chronological train/test split spanning the drift event | COVERED | 02-03 T1 |
| D-09 Concrete, real pandera Checks (not a passthrough schema) | COVERED | 02-04 T2 |
| D-09a Two-tier enforcement: structural hard-fail vs. logged row-level pre-filter | COVERED | 02-04 T1, T2 |
| Discretion: exact LightGBM hyperparameters | COVERED | 02-03 T2 |
| Discretion: internal `lib/` module boundaries and helper granularity | COVERED | 02-01 T3 |
| Discretion: benchmark methodology mechanics | COVERED | 02-05 T3 |
| Discretion: download-script mechanics | COVERED | 02-02 T1 |

**Result:** 0 MISSING items. Deferred Ideas in CONTEXT.md: none declared. No source item is left unplanned.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for pyshp and pyproj</name>
  <files>pyproject.toml (read-only at this point — nothing is installed until the verdict is recorded)</files>
  <what-built>Nothing yet. This gate runs before any dependency is added, because the two packages this phase introduces were flagged `[SUS]` by the automated package-legitimacy checker and neither appears in Phase 1's pinned `research/STACK.md`.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pyshp/ — expect the GeospatialPython/pyshp project, MIT licensed, a release history spanning many years, and a linked GitHub source repository.
    2. Open https://pypi.org/project/pyproj/ — expect the pyproj4/pyproj project, described as the official Python interface to PROJ, actively maintained.
    3. Confirm both look like the established projects described, not recently-created lookalikes with near-miss names.
  </how-to-verify>
  <resume-signal>Reply `approved` to let Task 2 install both into the `dev` optional-dependency group, or `rejected` plus the alternative you want. `rejected` halts this plan and plan 02-02 and returns the phase to planning; the only researched alternative is the geopandas/shapely stack, which D-06 explicitly rejected as too heavy for a one-time precomputation inside the 10-15h budget.</resume-signal>
  <read_first>
    - `.planning/phases/02-data-model-engineering-lib/02-RESEARCH.md` section `## Package Legitimacy Audit` (the verdict table and the two flagged rows)
    - `.planning/research/STACK.md` (confirm neither package appears in Phase 1's pinned list)
  </read_first>
  <prompt>
Two packages introduced by this phase were flagged `[SUS]` by the automated package-legitimacy checker and were NOT in Phase 1's pinned `research/STACK.md`:

- `pyshp` (imported as `shapefile`) — pure-Python ESRI shapefile reader, needed to read `taxi_zones.shp` polygon vertices for the D-06 zone-centroid precompute.
- `pyproj` — PROJ binding, needed to reproject those vertices from EPSG:2263 (US survey feet) to EPSG:4326 (degrees). RESEARCH.md verified this reprojection is load-bearing, not cosmetic.

RESEARCH.md notes the `[SUS]` verdict came from a checker with no PyPI download telemetry in this environment (`unknown-downloads` fired on every package, including pandas and numpy), so the verdict reflects a data gap rather than a legitimacy signal. It still needs human confirmation before install.

Please confirm each package at its registry page:
- https://pypi.org/project/pyshp/ — expect the GeospatialPython/pyshp project, MIT licensed, long release history.
- https://pypi.org/project/pyproj/ — expect the pyproj4/pyproj project, official PROJ binding.

Reply `approved` to proceed with installing both into the `dev` optional-dependency group, or `rejected` plus the alternative you want (the only researched alternative is `geopandas`/`shapely`, which D-06 explicitly rejected as too heavy).
  </prompt>
  <acceptance_criteria>
    - A human response of `approved` or `rejected` is recorded in the plan's SUMMARY before Task 2 installs anything.
    - This checkpoint is not auto-approved: `workflow.auto_advance` and `mode: yolo` do not apply to a package-legitimacy gate.
    - On `rejected`, Task 2 and plan 02-02 halt and the phase returns to planning rather than substituting a package silently.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-data-model-engineering-lib/02-01-SUMMARY.md &amp;&amp; grep -qiE 'pyshp.*(approved|rejected)' .planning/phases/02-data-model-engineering-lib/02-01-SUMMARY.md</automated>
  </verify>
  <done>A recorded human verdict on both packages exists; no install command has run before that verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Populate the ml dependency group and teach qa.sh + CI about it</name>
  <files>pyproject.toml, uv.lock, scripts/qa.sh, .github/workflows/ci.yml, .gitignore, README.md</files>
  <precondition>The working branch was created from `development` (not `master`) per `.claude/rules/requirements.md`; `git rev-parse --abbrev-ref HEAD` is not `master` and not `development`.</precondition>
  <read_first>
    - `pyproject.toml` (current `[project.optional-dependencies]` block, `[tool.ruff.lint] select`, `[tool.mypy] files`, `[tool.pytest.ini_options] addopts`, `[tool.coverage.run] branch`)
    - `scripts/qa.sh` (the `UV_RUN` array construction, both the CI and local branches, and the `UV_PROJECT_ENVIRONMENT` export)
    - `.github/workflows/ci.yml` (the three `uv sync --frozen --extra dev` steps in the `lint`, `typecheck` and `test` jobs)
    - `.gitignore` (current entries)
    - `.planning/phases/02-data-model-engineering-lib/02-RESEARCH.md` sections `## Standard Stack` (the Installation block with exact pins) and `### Pitfall 5`
    - `CLAUDE.MD` (the `UV_PROJECT_ENVIRONMENT=path/to/venv` mandate)
    - `README.md` lines about `--extra dev` and pre-commit/CI parity
  </read_first>
  <action>
Populate `pyproject.toml`'s currently-empty `ml` optional-dependency group with exactly the six runtime packages at the versions RESEARCH.md verified against the live PyPI index: `pandas>=2.3,<2.4`, `numpy>=2.5,<2.6`, `pyarrow`, `pandera==0.32.1`, `lightgbm==4.7.0`, `mlflow==3.15.1`. Use the plain `mlflow` distribution, not the extras variant, and do not add `boto3` — the extras/S3 client are needed only once a real MinIO-backed tracking server exists in Phase 3, and this phase's registry wrapper talks to a mocked client only.

Extend the existing `dev` group with `pandas-stubs` (required so `mypy --strict lib` can type pandas code instead of drowning in `Any`), plus `pyshp` and `pyproj` for the one-time zone-centroid precompute script. Keep `pyshp` and `pyproj` in `dev` and out of `ml`: per RESEARCH.md's Architectural Responsibility Map, they are repo-maintainer tooling that produces a committed static CSV, and `ml` is the group a future component image will ship. Leave the `pipeline` group empty and its Phase 3 comment intact. Preserve the existing explanatory comments above `pipeline` and `ml`, updating the `ml` comment only if it still says the group is to be filled.

Fix `scripts/qa.sh`: the `UV_RUN` array is built in two branches (a `CI`-set branch using `uv run --frozen --extra dev` and a local branch using `uv run --extra dev`). Add `--extra ml` to both branches so every subcommand — `lint`, `format`, `typecheck`, `test` — resolves against an environment containing pandas. Do not otherwise restructure the script: `REPO_ROOT` resolution, the `UV_PROJECT_ENVIRONMENT` export, the `boundary` echo line, and the `case` dispatch all stay exactly as they are.

Fix `.github/workflows/ci.yml`: each of the `lint`, `typecheck` and `test` jobs runs `uv sync --frozen --extra dev` before calling `scripts/qa.sh`. Add `--extra ml` to all three so CI installs the same dependency closure the script now runs against, preserving README.md's documented pre-commit/CI parity claim. Do not touch the `build-push` job.

Add `data/tlc/` to `.gitignore` so the D-07 monthly Parquet cache stays out of git, while leaving `data/zone_centroids.csv` trackable. Create the `data/` directory with a `.gitkeep` if git would otherwise not carry it.

Regenerate `uv.lock` so the new dependencies resolve, and install them into the venv at `path/to/venv` per CLAUDE.MD (`UV_PROJECT_ENVIRONMENT=path/to/venv uv sync --extra dev --extra ml`). CI runs `--frozen`, so a stale lockfile fails the build: commit the regenerated `uv.lock`.

Finally, update the `README.md` sentence that currently states every `uv` call inside the script passes `--extra dev`, so it names both extras. This is a one-sentence factual correction, not a rewrite.
  </action>
  <acceptance_criteria>
    - `grep -c 'extra ml' scripts/qa.sh` returns `2`.
    - `grep -v '^#' .github/workflows/ci.yml | grep -c 'uv sync --frozen --extra dev --extra ml'` returns `3`.
    - `python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb'))['project']['optional-dependencies']; ml=' '.join(d['ml']); dev=' '.join(d['dev']); assert all(p in ml for p in ('pandas','numpy','pyarrow','pandera','lightgbm','mlflow')), ml; assert 'pyshp' in dev and 'pyproj' in dev and 'pandas-stubs' in dev, dev; assert 'pyshp' not in ml and 'pyproj' not in ml, ml; print('ok')"` prints `ok`.
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml python -c "import pandas, numpy, pyarrow, pandera, lightgbm, mlflow, shapefile, pyproj; print(pandas.__version__)"` prints a `2.3.x` version string.
    - `grep -qx 'data/tlc/' .gitignore` succeeds.
    - `scripts/qa.sh lint`, `scripts/qa.sh format`, `scripts/qa.sh typecheck`, `scripts/qa.sh test` and `scripts/qa.sh boundary` all exit 0 on the unchanged Phase 1 tree.
    - `git status --porcelain uv.lock` shows the lockfile as modified and staged, not untracked-and-ignored.
  </acceptance_criteria>
  <verify>
    <automated>scripts/qa.sh lint &amp;&amp; scripts/qa.sh format &amp;&amp; scripts/qa.sh typecheck &amp;&amp; scripts/qa.sh test &amp;&amp; scripts/qa.sh boundary &amp;&amp; UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml python -c "import pandas, pandera, lightgbm, mlflow, shapefile, pyproj; print('deps-ok')"</automated>
  </verify>
  <done>Every `scripts/qa.sh` subcommand exits 0 with the `ml` group installed, all six ml packages plus the two dev-only geo packages import successfully inside the pinned venv, and `uv.lock` is regenerated and committed.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: End-to-end "one synthetic month becomes a scored model" — one path only</name>
  <files>lib/ingest.py, lib/schemas.py, lib/features.py, lib/train.py, lib/evaluate.py, lib/registry.py, tests/lib/test_tracer_end_to_end.py</files>
  <precondition>Task 2 has landed: `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml python -c "import pandas"` succeeds.</precondition>
  <reversibility rating="costly">Fixes the module boundary between "ingest reads and cleans" and "schemas validates", and the feature-frame column contract that train/evaluate consume. Changing it later means touching all six modules plus Phase 3's component split, but it is not irreversible.</reversibility>
  <read_first>
    - `lib/months.py` (module docstring convention, private `_`-prefixed helper style, `raise ValueError(f"... {value!r}")` error style — the only existing `lib/` module and the style all six new modules must match)
    - `tests/lib/test_months.py` (test style: one-line module docstring, plain `def test_x() -> None:` functions, no classes, exact-value asserts, `pytest.raises` for error paths)
    - `.planning/phases/02-data-model-engineering-lib/02-PATTERNS.md` sections `### lib/ingest.py`, `### lib/schemas.py`, `### lib/features.py`, `### lib/train.py`, `### lib/evaluate.py`, `### lib/registry.py`, and `## Shared Patterns`
    - `.planning/phases/02-data-model-engineering-lib/02-RESEARCH.md` sections `### Pattern 1` (chunked read), `### Pattern 3` (pandera schema), `### Pattern 4` (LightGBM config), `### Pattern 5` (MLflow wrapper), `### Vectorized Haversine (REQ-C2)`, `### dtype Downcasting (REQ-C3)`
    - `pyproject.toml` (`[tool.ruff.lint] select` includes `ANN`, so every function needs full annotations; `line-length = 100`; `--cov-fail-under=100` with `branch = true`)
  </read_first>
  <behavior>
    The end-to-end test in `tests/lib/test_tracer_end_to_end.py` must establish, before implementation:
    - Writing a ~40-row synthetic Yellow-schema DataFrame to a tmp-path Parquet file and reading it back with `read_month_chunked(path, batch_size=8)` returns every row, in source order, with the same column set.
    - `add_trip_duration(df)` adds a `trip_duration_s` float column equal to the dropoff-minus-pickup difference in seconds for a hand-computed row.
    - `validate_trips(df)` returns the frame unchanged for a clean synthetic month, and raises a pandera error class for a frame missing a required column.
    - `build_features(df, centroids)` on a 4-zone synthetic centroid table produces a frame whose columns are exactly `FEATURE_COLUMNS + [TARGET_COLUMN]`, and whose haversine column for a pair of identical zones is exactly `0.0`.
    - `train_trip_duration_model(x, y)` returns a fitted regressor whose `predict` returns one finite value per input row.
    - `rmse(y_true, y_pred)` on a hand-computed pair returns the exact expected float.
    - `ModelRegistry(mock_client, "trip-duration").get_champion_rmse()` returns `None` when the mocked client raises `MlflowException`, and a float when it returns a version whose tags carry an rmse value.
    - The full chain wired together — read -> duration -> validate -> features -> fit -> rmse -> champion lookup — yields a finite, strictly positive RMSE float.
  </behavior>
  <action>
Write the thinnest production-quality slice that runs one synthetic month all the way through every layer this phase owns. This is a tracer, not a prototype: the code written here is kept and expanded by plans 02-03/02-04/02-05, so it must be real (correct error handling on the single path, full type annotations, no placeholder returns), just narrow. Stubs are allowed only where a later plan can widen them without changing an architectural boundary — for example a schema with fewer Checks is fine, a schema replaced by an `if` chain is not.

Create six modules, each with a one-line module docstring in `lib/months.py`'s style. Cover only the symbols marked `[02-01]` in `## Artifacts this phase produces`; leave the remaining symbols for the expansion plans.

`lib/ingest.py`: `DEFAULT_BATCH_SIZE = 200_000`; `read_month_chunked(path: Path, batch_size: int = DEFAULT_BATCH_SIZE) -> pd.DataFrame` opening the file with `pyarrow.parquet.ParquetFile` and concatenating `iter_batches(batch_size=batch_size)` results with `ignore_index=True` — the chunked primitive is mandatory because the pandas whole-file loader has no chunk parameter and would defeat REQ-C4. `add_trip_duration(df: pd.DataFrame) -> pd.DataFrame` returns a copy with a `trip_duration_s` float column computed from `tpep_dropoff_datetime` minus `tpep_pickup_datetime` in seconds.

Also pin the REQ-D1 / D-07 window here, because both the download script (plan 02-02) and the chronological split (plan 02-05) must read the same source of truth: `TLC_START_MONTH = "2019-07"`, `TLC_END_MONTH = "2020-06"`, `TLC_DATA_DIR` pointing at `data/tlc` relative to the repo root, and `month_parquet_path(month: str, data_dir: Path = TLC_DATA_DIR) -> Path` returning `data_dir / f"yellow_tripdata_{month}.parquet"`. Add a short module comment recording why that window: twelve consecutive months whose final quarter crosses the March-2020 COVID demand collapse, which is the drift event REQ-D1 exists to demonstrate. Validate the month argument by delegating to `lib.months.month_range` semantics rather than writing a second month parser.

`lib/schemas.py`: a module-level pandera `DataFrameSchema` named `trip_schema` with `strict=False`, covering `tpep_pickup_datetime`, `tpep_dropoff_datetime`, `PULocationID`, `DOLocationID`, `trip_distance` and `passenger_count`, using real `Check`s per D-09 (zone IDs constrained to the 1-263 range, `trip_distance` strictly positive, `passenger_count` bounded and nullable). Expose `validate_trips(df: pd.DataFrame) -> pd.DataFrame` calling `trip_schema.validate(df, lazy=True)` and letting pandera's own error class propagate — do not wrap it in a hand-rolled exception. Plan 02-04 splits the enforcement into the two D-09a tiers; here, just get the schema object and the entry point in place.

`lib/features.py`: `EARTH_RADIUS_KM = 6371.0`; `ZONE_CENTROID_PATH` pointing at `data/zone_centroids.csv` relative to the repo root; `load_zone_centroids(path: Path = ZONE_CENTROID_PATH) -> pd.DataFrame` reading columns `zone_id`, `centroid_lat`, `centroid_lon` with pandas only; `haversine_km(lat1, lon1, lat2, lon2) -> pd.Series` fully vectorized over numpy ufuncs; `FEATURE_COLUMNS` and `TARGET_COLUMN = "trip_duration_s"`; and `build_features(df, centroids) -> pd.DataFrame` that left-joins the centroid table twice (once on `PULocationID`, once on `DOLocationID`), computes a `trip_distance_km` haversine column, and returns exactly `FEATURE_COLUMNS + [TARGET_COLUMN]`. Fix `FEATURE_COLUMNS` here at its final Phase 2 value — `["PULocationID", "DOLocationID", "VendorID", "passenger_count", "trip_distance", "trip_distance_km", "pickup_hour", "pickup_dayofweek"]` — deriving `pickup_hour` and `pickup_dayofweek` from `tpep_pickup_datetime`; pinning the full column list in the tracer is what lets the later plans expand `lib/train.py` and `lib/features.py` in different waves without renegotiating the training contract. Clamp the haversine `arcsin` argument into `[0.0, 1.0]` so identical coordinates return exactly `0.0` rather than a NaN from a marginally-negative floating-point radicand. This module must read the centroid table with pandas alone; the shapefile and reprojection tooling stays in the precompute script that plan 02-02 writes.

`lib/train.py`: `CATEGORICAL_FEATURES = ["PULocationID", "DOLocationID", "VendorID"]`; a module-level `LGBM_PARAMS` dict holding one fixed, documented configuration (`objective="regression"`, `metric="rmse"`, `num_leaves=31`, `learning_rate=0.05`, `n_estimators=300`, `random_state=42`, `n_jobs=1`, `verbose=-1`) with a short comment stating these are fixed by REQ-D2 and deliberately untuned; and `train_trip_duration_model(x: pd.DataFrame, y: pd.Series) -> lgb.LGBMRegressor` that casts the categorical columns and fits. Introduce no hyperparameter-search or sweep library.

`lib/evaluate.py`: `rmse(y_true, y_pred) -> float` computing the root mean squared error with numpy and returning a plain Python float.

`lib/registry.py`: `CHAMPION_ALIAS = "champion"`, `RMSE_TAG = "rmse"`, and a `ModelRegistry` class taking an injected client plus a model name, exposing `get_champion_rmse() -> float | None` that calls `get_model_version_by_alias(model_name, CHAMPION_ALIAS)` and returns `None` when the client raises `MlflowException`, otherwise the float value of the version's rmse tag. Dependency-inject the client through `__init__` so tests never construct a real one — no MLflow server exists until Phase 3.

Then write `tests/lib/test_tracer_end_to_end.py` implementing the `<behavior>` list above, using tiny synthetic in-test DataFrames per D-07 (never the real downloaded Parquet) and `unittest.mock.MagicMock` for the MLflow client per RESEARCH.md Pattern 5. Write the tests first and watch them fail, then implement until green. Add exactly the extra small unit tests needed for any branch the end-to-end path does not reach — `pyproject.toml` enforces 100% branch coverage over `lib/`, so an untested exception branch fails the whole suite, not just its module.

Keep every module free of `kfp` and `kfp_kubernetes` imports, and add no pandas import under `components/` — `scripts/check_component_boundary.sh` must keep passing.
  </action>
  <acceptance_criteria>
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml pytest tests/lib/test_tracer_end_to_end.py --no-cov -q` exits 0.
    - The end-to-end test asserts `math.isfinite(result_rmse) and result_rmse > 0`, where `result_rmse` came from the full read -> duration -> validate -> features -> fit -> rmse chain (not a hardcoded constant).
    - `python -c "import ast,sys; mods={'ingest':['read_month_chunked','add_trip_duration','DEFAULT_BATCH_SIZE','TLC_START_MONTH','TLC_END_MONTH','TLC_DATA_DIR','month_parquet_path'],'schemas':['trip_schema','validate_trips'],'features':['load_zone_centroids','haversine_km','build_features','FEATURE_COLUMNS','TARGET_COLUMN','EARTH_RADIUS_KM','ZONE_CENTROID_PATH'],'train':['train_trip_duration_model','LGBM_PARAMS','CATEGORICAL_FEATURES'],'evaluate':['rmse'],'registry':['ModelRegistry','CHAMPION_ALIAS','RMSE_TAG']}; src={m:open(f'lib/{m}.py').read() for m in mods}; [sys.exit(f'missing {m}.{n}') for m,ns in mods.items() for n in ns if n not in src[m]]; print('symbols-ok')"` prints `symbols-ok`.
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml python -c "from lib.months import month_range; from lib.ingest import TLC_START_MONTH, TLC_END_MONTH; m = month_range(TLC_START_MONTH, TLC_END_MONTH); assert len(m) == 12, m; assert '2020-03' in m, m; print('window-ok')"` prints `window-ok`.
    - A test asserts `build_features` returns a haversine value of exactly `0.0` for a row whose pickup and dropoff zone are the same zone id.
    - A test asserts `ModelRegistry.get_champion_rmse()` is `None` when the mocked client raises the MLflow exception type, and equals a specific float when it returns a tagged version.
    - `UV_PROJECT_ENVIRONMENT=path/to/venv uv run --extra dev --extra ml python -c "import ast, pathlib; bad=[p.name for p in pathlib.Path('lib').glob('*.py') for n in ast.walk(ast.parse(p.read_text())) if (isinstance(n, ast.Import) and any(a.name.split('.')[0].startswith('kfp') for a in n.names)) or (isinstance(n, ast.ImportFrom) and (n.module or '').split('.')[0].startswith('kfp'))]; assert not bad, bad; print('no-cluster-imports-ok')"` prints `no-cluster-imports-ok`.
    - `scripts/qa.sh test` exits 0, meaning the 100% branch-coverage gate is met with all six new modules present.
    - `scripts/qa.sh typecheck` exits 0 under `mypy --strict`.
    - `scripts/qa.sh boundary` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>scripts/qa.sh lint &amp;&amp; scripts/qa.sh format &amp;&amp; scripts/qa.sh typecheck &amp;&amp; scripts/qa.sh test &amp;&amp; scripts/qa.sh boundary</automated>
  </verify>
  <done>One synthetic month travels the whole stack end to end and produces a finite positive RMSE compared against a mocked champion lookup; all five quality gates pass; the six module interfaces are committed and stable for the wave-2 and wave-3 expansion plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> developer machine / CI runner | Third-party package code executes at install and import time |
| TLC CloudFront -> `lib/ingest.py` | Externally-controlled Parquet bytes become in-process DataFrame structure and dtypes |
| Model artifact file -> `lib/train.py` loader | Serialized model bytes become executable object graph if a pickle-style format is used |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | `uv add`/`uv sync` installs of `pyshp` and `pyproj` (both `[SUS]` per RESEARCH.md, neither in Phase 1's STACK.md) | high | mitigate | Task 1 is a `blocking-human` package-legitimacy checkpoint verified against pypi.org before Task 2 installs anything; not auto-approvable regardless of `mode: yolo` |
| T-02-01 | Tampering | `lib/ingest.py` reading externally-sourced Parquet | medium | mitigate | Structure and dtypes are asserted by `lib/schemas.py`'s pandera schema (ASVS V5 input validation) before any downstream module trusts the frame; expanded to the full two-tier gate in plan 02-03 |
| T-02-05 | Denial of Service | `lib/ingest.py` memory footprint on a 16GB machine | medium | mitigate | `read_month_chunked` uses `pyarrow.parquet.ParquetFile.iter_batches(batch_size=...)`, bounding peak Arrow-batch residency instead of materializing the whole file at once (REQ-C4) |
| T-02-02 | Information Disclosure | `lib/registry.py` credential handling | low | accept | Phase 2 injects a mocked client and handles no tracking-server URI, token or S3 credential; real credential handling is scoped to Phase 3 when a server exists |
</threat_model>

<verification>
- `scripts/qa.sh lint`, `format`, `typecheck`, `test` and `boundary` all exit 0.
- `pre-commit run --all-files` reaches the same verdict as the five `scripts/qa.sh` subcommands (README.md's parity claim still holds after the CI workflow change).
- Both `pyshp` and `pyproj` appear only under `dev`, never under `ml`.
- The tracer test drives all six modules and produces a real RMSE.
</verification>

<success_criteria>
- `pyproject.toml`'s `ml` group holds exactly the six RESEARCH.md-verified pins; `dev` gains `pandas-stubs`, `pyshp`, `pyproj`.
- `scripts/qa.sh` and all three checking CI jobs install and run against `--extra dev --extra ml`.
- Six `lib/` modules exist with the `[02-01]` symbol set, pass `mypy --strict`, and hold 100% branch coverage.
- `tests/lib/test_tracer_end_to_end.py` asserts a finite positive RMSE produced by the full chain.
- `data/tlc/` is gitignored; `data/zone_centroids.csv` remains trackable.
</success_criteria>

<output>
Create `.planning/phases/02-data-model-engineering-lib/02-01-SUMMARY.md` when done. Record the Task 1 human verdict verbatim, the exact resolved versions `uv.lock` pinned for the six `ml` packages, and the final public symbol list per module so the wave-2 plans can rely on it.
</output>
